**2장 Python 기초 — 2.2 자료구조**

# 2.2 자료구조 (Data Structures)

리스트 · 튜플 · 세트 · 딕셔너리

*파이썬 인공지능 풀스택 · pp.46~91 중 2.2*

> Python은 데이터를 담는 방식에 따라 네 가지 내장 자료구조를 제공한다. 리스트는 순서가 있고 자유롭게 바뀌는 목록, 튜플은 리스트와 비슷하지만 한 번 만들면 바뀌지 않는 목록, 세트는 순서와 중복을 허용하지 않는 수학적 집합, 딕셔너리는 키-값 쌍으로 데이터를 저장하는 구조다. 이 절에서는 각 자료구조의 선언·접근·주요 메서드를 예제로 익히고, 서로의 차이를 표로 비교한다.

---

## 2.2.1 리스트 (List)

- **정의**: 여러 값을 하나의 변수에 저장하는 순차적 자료형 — 대괄호 `[ ]`로 선언
- **특징**: 순서 있음 · 다양한 자료형 포함 가능 · 변경 가능(Mutable) · 중복 허용
- **접근 방법**: 인덱싱(0부터 시작, 음수 인덱스 지원) · 슬라이싱(`[시작:끝:간격]`)
- **주요 메서드**: `append() · insert() · remove() · pop() · sort() · reverse() · len()`

---

**① 리스트 선언**

**예제 코드**: `basic23.py`

```python
fruits = ["apple", "banana", "cherry"]
print(fruits)   # 출력: ['apple', 'banana', 'cherry']
```

<details>
<summary><span class="label-badge">코드분석</span></summary>

```python
"""
2.2.1 리스트 — 대괄호로 리스트 선언
"""

fruits = ["apple", "banana", "cherry"]   # 대괄호 []로 3개의 문자열을 담은 리스트 생성
print(fruits)   # 리스트를 통째로 출력하면 대괄호와 함께 원소가 그대로 보인다

# ─────────────────────────────────────────────────────────────
# [교안용 설명 포인트]
# 1) 리스트는 서로 다른 타입을 섞어 담을 수도 있다는 점(예: [1, "a", True])을
#    함께 언급하면 좋다.
# ─────────────────────────────────────────────────────────────
```

</details>

*문자열 3개를 담은 리스트가 대괄호 형태 그대로 출력된다.*

**실행 결과**: `basic23.py`

```
['apple', 'banana', 'cherry']
```

---

**② 리스트 인덱싱**

**예제 코드**: `basic24.py`

```python
fruits = ["apple", "banana", "cherry"]
print(fruits[0])    # 출력: apple
print(fruits[-1])   # 출력: cherry
```

<details>
<summary><span class="label-badge">코드분석</span></summary>

```python
"""
2.2.1 리스트 — 인덱싱(양수/음수)
"""

fruits = ["apple", "banana", "cherry"]

print(fruits[0])    # 인덱스는 0부터 시작 → 첫 번째 원소 apple
print(fruits[-1])   # 음수 인덱스는 뒤에서부터 셈 → -1은 마지막 원소 cherry

# ─────────────────────────────────────────────────────────────
# [교안용 설명 포인트]
# 1) fruits[3]처럼 존재하지 않는 인덱스를 쓰면 IndexError가 발생한다는
#    것을 실습으로 보여주면 좋다.
# ─────────────────────────────────────────────────────────────
```

</details>

*인덱스 0은 첫 원소(apple), 인덱스 -1은 마지막 원소(cherry)를 가리킨다.*

**실행 결과**: `basic24.py`

```
apple
cherry
```

---

**③ 리스트 슬라이싱**

**예제 코드**: `basic25.py`

```python
fruits = ["apple", "banana", "cherry", "date", "elderberry"]
print(fruits[1:4])   # 출력: ['banana', 'cherry', 'date']
print(fruits[:3])    # 출력: ['apple', 'banana', 'cherry']
print(fruits[::2])   # 출력: ['apple', 'cherry', 'elderberry']
```

<details>
<summary><span class="label-badge">코드분석</span></summary>

```python
"""
2.2.1 리스트 — 슬라이싱 [시작:끝:간격]
"""

fruits = ["apple", "banana", "cherry", "date", "elderberry"]
# 인덱스:            0        1        2       3      4

print(fruits[1:4])   # 인덱스 1부터 3까지(4는 미포함) → banana, cherry, date
print(fruits[:3])    # 시작 생략 → 0부터 2까지 → apple, banana, cherry
print(fruits[::2])   # 간격 2 → 0, 2, 4번째 원소 → apple, cherry, elderberry

# ─────────────────────────────────────────────────────────────
# [교안용 설명 포인트]
# 1) 슬라이싱은 range()와 마찬가지로 끝 인덱스를 포함하지 않는다는
#    규칙을 다시 한 번 강조 — [1:4]는 인덱스 4가 아니라 1,2,3까지.
# 2) fruits[::-1]로 리스트를 뒤집을 수 있다는 응용도 함께 소개하면 좋다.
# ─────────────────────────────────────────────────────────────
```

</details>

*슬라이싱의 끝 인덱스는 포함되지 않으며, 간격을 지정하면 그 간격만큼 건너뛰며 값을 뽑아낸다.*

**실행 결과**: `basic25.py`

```
['banana', 'cherry', 'date']
['apple', 'banana', 'cherry']
['apple', 'cherry', 'elderberry']
```

---

**④ 리스트 주요 메서드**

**예제 코드**: `basic26.py`

```python
# append()
fruits = ["apple", "banana"]
fruits.append("cherry")
print(fruits)   # 출력: ['apple', 'banana', 'cherry']

# insert()
fruits.insert(1, "orange")   # 두 번째 위치에 "orange" 추가
print(fruits)   # 출력: ['apple', 'orange', 'banana', 'cherry']

# remove()
fruits.remove("banana")   # "banana" 삭제
print(fruits)   # 출력: ['apple', 'orange', 'cherry']

# pop()
removed_fruit = fruits.pop(1)   # 두 번째 요소 제거하고 반환
print(removed_fruit)   # 출력: orange
print(fruits)   # 출력: ['apple', 'cherry']

# sort()
fruits = ["cherry", "banana", "apple"]
fruits.sort()
print(fruits)   # 출력: ['apple', 'banana', 'cherry']

# reverse()
fruits.reverse()
print(fruits)   # 출력: ['cherry', 'banana', 'apple']
```

<details>
<summary><span class="label-badge">코드분석</span></summary>

```python
"""
2.2.1 리스트 — 주요 메서드 6종(append/insert/remove/pop/sort/reverse)

리스트 메서드는 대부분 반환값 없이(None) 리스트 자체를 직접 바꾼다
(제자리 변경, in-place) — pop()만 예외적으로 제거한 값을 반환한다.
"""

fruits = ["apple", "banana"]
fruits.append("cherry")        # 끝에 원소 하나 추가
print(fruits)                  # ['apple', 'banana', 'cherry']

fruits.insert(1, "orange")     # 인덱스 1 위치에 삽입, 뒤 원소들은 밀림
print(fruits)                  # ['apple', 'orange', 'banana', 'cherry']

fruits.remove("banana")        # 값으로 찾아서 첫 번째로 일치하는 원소 삭제
print(fruits)                  # ['apple', 'orange', 'cherry']

removed_fruit = fruits.pop(1)  # 인덱스 1의 원소를 제거하면서 그 값을 반환
print(removed_fruit)           # orange
print(fruits)                  # ['apple', 'cherry']

fruits = ["cherry", "banana", "apple"]
fruits.sort()                  # 알파벳(사전) 순으로 리스트 자체를 정렬
print(fruits)                  # ['apple', 'banana', 'cherry']

fruits.reverse()               # 현재 순서를 그대로 뒤집음
print(fruits)                  # ['cherry', 'banana', 'apple']

# ─────────────────────────────────────────────────────────────
# [교안용 설명 포인트]
# 1) sort()는 리스트 자체를 바꾸고 반환값이 None이다 — sorted(fruits)는
#    원본은 그대로 두고 정렬된 "새 리스트"를 반환한다는 차이를 꼭 짚는다.
# 2) remove()는 값이 없으면 ValueError, pop()에 범위 밖 인덱스를 주면
#    IndexError가 난다는 것도 실습으로 확인해보면 좋다.
# ─────────────────────────────────────────────────────────────
```

</details>

*append/insert/remove/pop은 리스트의 내용을 직접 바꾸고, pop()만 제거한 값을 반환한다. sort()/reverse() 역시 리스트 자체를 제자리에서 바꾼다.*

**실행 결과**: `basic26.py`

```
['apple', 'banana', 'cherry']
['apple', 'orange', 'banana', 'cherry']
['apple', 'orange', 'cherry']
orange
['apple', 'cherry']
['apple', 'banana', 'cherry']
['cherry', 'banana', 'apple']
```

---

**⑤ 리스트의 중첩 (Nested List)**

**예제 코드**: `basic27.py`

```python
matrix = [[1, 2, 3], [4, 5, 6], [7, 8, 9]]
print(matrix[0])       # 출력: [1, 2, 3]
print(matrix[1][2])    # 출력: 6
```

<details>
<summary><span class="label-badge">코드분석</span></summary>

```python
"""
2.2.1 리스트 — 리스트 안에 리스트(2차원 배열 흉내)
"""

matrix = [[1, 2, 3], [4, 5, 6], [7, 8, 9]]   # 3개의 리스트를 원소로 갖는 리스트

print(matrix[0])      # 바깥 인덱스 0 → 첫 번째 행(리스트) [1, 2, 3]
print(matrix[1][2])   # matrix[1]은 [4, 5, 6], 그 중 인덱스 2 → 6

# ─────────────────────────────────────────────────────────────
# [교안용 설명 포인트]
# 1) matrix[1][2]처럼 대괄호를 두 번 쓰는 것은 "행을 먼저 고르고, 그
#    행 안에서 다시 열을 고른다"는 뜻이다 — 2.5.1 NumPy의 2D 배열
#    개념과 자연스럽게 연결된다.
# ─────────────────────────────────────────────────────────────
```

</details>

*리스트 안에 리스트를 넣으면 행렬처럼 2차원 데이터를 표현할 수 있고, 대괄호를 두 번 이어 써서 행과 열을 차례로 지정한다.*

**실행 결과**: `basic27.py`

```
[1, 2, 3]
6
```

---

**⑥ 리스트 반복문 사용**

**예제 코드**: `basic28.py`

```python
fruits = ["apple", "banana", "cherry"]
for fruit in fruits:
    print(fruit)
```

<details>
<summary><span class="label-badge">코드분석</span></summary>

```python
"""
2.2.1 리스트 — for 문으로 리스트 순회 (2.1.4 반복문 복습)
"""

fruits = ["apple", "banana", "cherry"]
for fruit in fruits:   # 리스트의 각 원소를 순서대로 fruit에 대입
    print(fruit)

# ─────────────────────────────────────────────────────────────
# [교안용 설명 포인트]
# 1) for i in range(len(fruits)): print(fruits[i]) 방식과 비교해,
#    Python에서는 인덱스 없이 원소 자체를 바로 꺼내 쓰는 방식이
#    더 널리 쓰인다는 점을 강조한다.
# ─────────────────────────────────────────────────────────────
```

</details>

*리스트를 for 문으로 순회하면 원소가 선언된 순서 그대로 출력된다.*

**실행 결과**: `basic28.py`

```
apple
banana
cherry
```

---

## 2.2.2 튜플 (Tuple)

- **정의**: 순서가 있는 데이터 구조 — 소괄호 `( )`로 선언, 리스트와 유사하나 불변(Immutable)
- **특징**: 생성 후 요소 변경 불가 · 순서 있음 · 다양한 자료형 포함 · 중복 허용 · 해시 가능(딕셔너리 키로 사용 가능)
- **주요 메서드**: `count() · index()` — 불변 자료형이라 추가/삭제 메서드는 제공되지 않음
- **사용 시점**: 데이터 불변성 보장이 필요할 때, 리스트보다 성능이 중요할 때, 딕셔너리 키로 사용할 때

---

**① 튜플 선언**

**예제 코드**: `basic29.py`

```python
fruits = ("apple", "banana", "cherry")
print(fruits)   # 출력: ('apple', 'banana', 'cherry')
```

<details>
<summary><span class="label-badge">코드분석</span></summary>

```python
"""
2.2.2 튜플 — 소괄호로 튜플 선언
"""

fruits = ("apple", "banana", "cherry")   # 소괄호 ()로 선언 → 이후 요소 변경 불가
print(fruits)

# ─────────────────────────────────────────────────────────────
# [교안용 설명 포인트]
# 1) fruits[0] = "kiwi"처럼 값을 바꾸려 하면 TypeError: 'tuple' object
#    does not support item assignment가 발생한다는 것을 직접 실습해보면
#    "불변"의 의미가 체감된다.
# ─────────────────────────────────────────────────────────────
```

</details>

*소괄호로 선언한 튜플은 리스트와 겉모습만 다를 뿐 순서 있는 값의 모음이라는 점은 같지만, 생성 이후에는 값을 바꿀 수 없다.*

**실행 결과**: `basic29.py`

```
('apple', 'banana', 'cherry')
```

---

**② 튜플 인덱싱**

**예제 코드**: `basic30.py`

```python
fruits = ("apple", "banana", "cherry")
print(fruits[0])    # 출력: apple
print(fruits[-1])   # 출력: cherry
```

<details>
<summary><span class="label-badge">코드분석</span></summary>

```python
"""
2.2.2 튜플 — 인덱싱(리스트와 동일한 규칙)
"""

fruits = ("apple", "banana", "cherry")
print(fruits[0])    # apple — 인덱싱 규칙은 리스트와 완전히 동일
print(fruits[-1])   # cherry

# ─────────────────────────────────────────────────────────────
# [교안용 설명 포인트]
# 1) 인덱싱·슬라이싱처럼 "읽기" 동작은 리스트와 똑같이 지원되고,
#    append·remove처럼 "쓰기" 동작만 지원되지 않는다는 것이 튜플의
#    핵심이다.
# ─────────────────────────────────────────────────────────────
```

</details>

*값을 읽는 인덱싱은 리스트와 동일하게 동작한다 — 불변인 것은 "쓰기"뿐이다.*

**실행 결과**: `basic30.py`

```
apple
cherry
```

---

**③ 튜플 슬라이싱**

**예제 코드**: `basic31.py`

```python
fruits = ("apple", "banana", "cherry", "date", "elderberry")
print(fruits[1:4])   # 출력: ('banana', 'cherry', 'date')
print(fruits[:3])    # 출력: ('apple', 'banana', 'cherry')
print(fruits[::2])   # 출력: ('apple', 'cherry', 'elderberry')
```

<details>
<summary><span class="label-badge">코드분석</span></summary>

```python
"""
2.2.2 튜플 — 슬라이싱(결과도 튜플로 반환)
"""

fruits = ("apple", "banana", "cherry", "date", "elderberry")

print(fruits[1:4])   # ('banana', 'cherry', 'date') — 슬라이싱 결과도 튜플
print(fruits[:3])    # ('apple', 'banana', 'cherry')
print(fruits[::2])   # ('apple', 'cherry', 'elderberry')

# ─────────────────────────────────────────────────────────────
# [교안용 설명 포인트]
# 1) 리스트를 슬라이싱하면 리스트가, 튜플을 슬라이싱하면 튜플이
#    나온다 — "원본 자료형을 유지한다"는 규칙을 강조한다.
# ─────────────────────────────────────────────────────────────
```

</details>

*리스트와 동일한 슬라이싱 규칙이 적용되며, 결과 역시 원본과 같은 튜플 타입으로 반환된다.*

**실행 결과**: `basic31.py`

```
('banana', 'cherry', 'date')
('apple', 'banana', 'cherry')
('apple', 'cherry', 'elderberry')
```

---

**④ 튜플 주요 메서드 (count/index)**

**예제 코드**: `basic32.py`

```python
fruits = ("apple", "banana", "cherry", "banana")

# count() 사용 예
print(fruits.count("banana"))   # 출력: 2

# index() 사용 예
print(fruits.index("cherry"))   # 출력: 2
```

<details>
<summary><span class="label-badge">코드분석</span></summary>

```python
"""
2.2.2 튜플 — count()/index() (불변이라 이 두 개만 제공됨)
"""

fruits = ("apple", "banana", "cherry", "banana")
# 인덱스:      0         1        2        3

print(fruits.count("banana"))   # "banana"가 몇 번 등장하는지 → 2
print(fruits.index("cherry"))   # "cherry"가 처음 등장하는 인덱스 → 2

# ─────────────────────────────────────────────────────────────
# [교안용 설명 포인트]
# 1) 튜플은 요소를 추가·삭제할 수 없으므로 append/remove 같은
#    "변경" 메서드 자체가 아예 존재하지 않는다 — count/index처럼
#    "조회"만 가능한 메서드만 남아 있다는 점을 강조한다.
# ─────────────────────────────────────────────────────────────
```

</details>

*count()는 값의 등장 횟수를, index()는 값이 처음 등장하는 위치를 알려준다. 튜플에는 이 두 조회용 메서드만 있고 값을 바꾸는 메서드는 없다.*

**실행 결과**: `basic32.py`

```
2
2
```

---

**⑤ 튜플의 중첩 (Nested Tuple)**

**예제 코드**: `basic33.py`

```python
nested_tuple = ((1, 2, 3), (4, 5, 6), (7, 8, 9))
print(nested_tuple[1])       # 출력: (4, 5, 6)
print(nested_tuple[1][2])    # 출력: 6
```

<details>
<summary><span class="label-badge">코드분석</span></summary>

```python
"""
2.2.2 튜플 — 튜플 안에 튜플(중첩 구조)
"""

nested_tuple = ((1, 2, 3), (4, 5, 6), (7, 8, 9))

print(nested_tuple[1])      # 두 번째 튜플 (4, 5, 6)
print(nested_tuple[1][2])   # 그 안의 세 번째 값 → 6

# ─────────────────────────────────────────────────────────────
# [교안용 설명 포인트]
# 1) basic27의 중첩 리스트(matrix)와 완전히 같은 방식으로 동작한다는
#    점을 비교해 짚어주면, "중첩 구조"라는 개념 자체가 자료형과
#    무관하게 동일함을 이해시킬 수 있다.
# ─────────────────────────────────────────────────────────────
```

</details>

*튜플도 리스트처럼 중첩 구조를 만들 수 있으며, 대괄호를 이어 써서 바깥→안쪽 순서로 접근한다.*

**실행 결과**: `basic33.py`

```
(4, 5, 6)
6
```

---

**⑥ 튜플 반복문 사용**

**예제 코드**: `basic34.py`

```python
fruits = ("apple", "banana", "cherry")
for fruit in fruits:
    print(fruit)
```

<details>
<summary><span class="label-badge">코드분석</span></summary>

```python
"""
2.2.2 튜플 — for 문으로 튜플 순회
"""

fruits = ("apple", "banana", "cherry")
for fruit in fruits:   # 튜플도 리스트와 동일하게 for로 순회 가능
    print(fruit)

# ─────────────────────────────────────────────────────────────
# [교안용 설명 포인트]
# 1) 순회(iteration) 관점에서는 리스트와 튜플이 완전히 동등하다 —
#    차이는 오직 "변경 가능 여부"뿐이라는 점을 다시 한 번 정리한다.
# ─────────────────────────────────────────────────────────────
```

</details>

*튜플 역시 for 문으로 순서대로 순회할 수 있다.*

**실행 결과**: `basic34.py`

```
apple
banana
cherry
```

---

**튜플과 리스트의 차이점**

| 특징 | 튜플 (Tuple) | 리스트 (List) |
|---|---|---|
| 변경 가능성 | 불변 (Immutable) | 가변 (Mutable) |
| 괄호 종류 | 소괄호 `()` | 대괄호 `[]` |
| 성능 | 더 빠름 (불변성 덕분에) | 상대적으로 느림 |
| 사용 사례 | 변경되지 않는 데이터에 적합 | 요소 변경이 필요한 데이터에 적합 |
| 예시 | `(1, 2, 3)` | `[1, 2, 3]` |

---

## 2.2.3 세트 (Set)

- **정의**: 중복되지 않는 요소들로 구성된 순서 없는 데이터 구조 — 수학적 집합을 구현
- **특징**: 순서 없음(인덱스 접근 불가) · 중복 불허(자동 제거) · 변경 가능(Mutable) · 집합 연산 지원
- **집합 연산**: 합집합(`|`) · 교집합(`&`) · 차집합(`-`) · 대칭차집합(`^`)
- **주요 메서드**: `add() · remove() · discard() · pop() · clear() · len()`

---

**① 세트 선언**

**예제 코드**: `basic35.py`

```python
fruits = {"apple", "banana", "cherry"}
print(fruits)   # 출력: {'apple', 'banana', 'cherry'}
```

<details>
<summary><span class="label-badge">코드분석</span></summary>

```python
"""
2.2.3 세트 — 중괄호로 세트 선언

딕셔너리도 중괄호 {}를 쓰지만, 안에 콜론(:)이 없는 {값, 값, ...}
형태는 세트로 해석된다.
"""

fruits = {"apple", "banana", "cherry"}   # 중괄호 {} + 콜론 없음 → 세트
print(fruits)   # 세트는 "순서가 없다" — 저장한 순서와 다르게 출력될 수 있음

# ─────────────────────────────────────────────────────────────
# [교안용 설명 포인트]
# 1) 원본 주석의 "# 출력: {'apple', 'banana', 'cherry'}"와 실제 실행
#    결과 "{'cherry', 'banana', 'apple'}"의 순서가 다른데, 이것이
#    출력 오류가 아니라 "세트는 순서를 보장하지 않는다"는 특성 그
#    자체라는 점을 반드시 짚어줘야 한다.
# 2) 빈 세트를 만들 때는 {}가 아니라 set()을 써야 한다는 것도 함께
#    안내하면 좋다 — {}는 빈 딕셔너리로 해석되기 때문이다.
# ─────────────────────────────────────────────────────────────
```

</details>

*세트는 순서를 보장하지 않으므로, 코드에 적은 순서와 실제 출력 순서가 다를 수 있다(실행 결과에서 apple·banana·cherry의 순서가 바뀐 것도 그 때문이다).*

**실행 결과**: `basic35.py`

```
{'cherry', 'banana', 'apple'}
```

---

**② 세트의 중복 제거**

**예제 코드**: `basic36.py`

```python
fruits = {"apple", "banana", "cherry", "apple"}
print(fruits)   # 출력: {'apple', 'banana', 'cherry'}
```

<details>
<summary><span class="label-badge">코드분석</span></summary>

```python
"""
2.2.3 세트 — 중복 값 자동 제거
"""

fruits = {"apple", "banana", "cherry", "apple"}   # "apple"을 두 번 적었지만
print(fruits)   # 세트는 중복을 허용하지 않아 자동으로 하나만 남는다

# ─────────────────────────────────────────────────────────────
# [교안용 설명 포인트]
# 1) 리스트에 중복이 있는 데이터를 set(리스트)로 감싸면 손쉽게
#    중복을 제거할 수 있다는 실무 활용 팁을 함께 소개하면 좋다
#    (예: unique = list(set(원본리스트))).
# ─────────────────────────────────────────────────────────────
```

</details>

*"apple"을 두 번 넣었지만 세트는 중복을 자동으로 제거해 세 개의 값만 남는다.*

**실행 결과**: `basic36.py`

```
{'cherry', 'banana', 'apple'}
```

---

**③ 세트 반복문 순회**

**예제 코드**: `basic37.py`

```python
fruits = {"apple", "banana", "cherry"}
for fruit in fruits:
    print(fruit)
```

<details>
<summary><span class="label-badge">코드분석</span></summary>

```python
"""
2.2.3 세트 — for 문으로 세트 순회(순서 보장 안 됨)
"""

fruits = {"apple", "banana", "cherry"}
for fruit in fruits:   # 세트도 순회 가능하지만, 순서는 정해져 있지 않음
    print(fruit)

# ─────────────────────────────────────────────────────────────
# [교안용 설명 포인트]
# 1) 리스트/튜플의 for 순회는 항상 저장 순서대로지만, 세트는 실행할
#    때마다(또는 Python 버전에 따라) 순서가 달라질 수 있다는 것을
#    강조한다 — "순서에 의존하는 로직에는 세트를 쓰면 안 된다."
# ─────────────────────────────────────────────────────────────
```

</details>

*세트를 순회한 결과의 순서는 실행할 때마다 달라질 수 있다는 점에서 리스트·튜플과 근본적으로 다르다.*

**실행 결과**: `basic37.py`

```
cherry
banana
apple
```

---

**④ 세트 집합 연산 (합/교/차/대칭차집합)**

**예제 코드**: `basic38.py`

```python
set1 = {1, 2, 3, 4}
set2 = {3, 4, 5, 6}

# 합집합
union_set = set1 | set2
print(union_set)   # 출력: {1, 2, 3, 4, 5, 6}

# 교집합
intersection_set = set1 & set2
print(intersection_set)   # 출력: {3, 4}

# 차집합
difference_set = set1 - set2
print(difference_set)   # 출력: {1, 2}

# 대칭 차집합 (Symmetric Difference)
symmetric_difference_set = set1 ^ set2
print(symmetric_difference_set)   # 출력: {1, 2, 5, 6}
```

<details>
<summary><span class="label-badge">코드분석</span></summary>

```python
"""
2.2.3 세트 — 집합 연산 4종(합/교/차/대칭차집합)

수학 시간에 배운 벤 다이어그램의 합집합·교집합·차집합·대칭차집합을
Python 연산자로 그대로 계산한다.
"""

set1 = {1, 2, 3, 4}
set2 = {3, 4, 5, 6}

union_set = set1 | set2          # 합집합: 둘 중 하나라도 속하면 포함 → {1,2,3,4,5,6}
print(union_set)

intersection_set = set1 & set2   # 교집합: 둘 다에 속하는 값만 → {3,4}
print(intersection_set)

difference_set = set1 - set2     # 차집합: set1에만 있고 set2에는 없는 값 → {1,2}
print(difference_set)

symmetric_difference_set = set1 ^ set2   # 대칭차집합: 어느 한쪽에만 있는 값(양쪽 겹치는 3,4는 제외) → {1,2,5,6}
print(symmetric_difference_set)

# ─────────────────────────────────────────────────────────────
# [교안용 설명 포인트]
# 1) set1 - set2와 set2 - set1은 결과가 다르다(방향성이 있음) — 실습으로
#    set2 - set1 = {5, 6}이 됨을 직접 비교시켜주면 좋다.
# 2) 대칭차집합(^)은 "합집합에서 교집합을 뺀 것"과 같다는 것을
#    (set1 | set2) - (set1 & set2)로 검증해보게 하면 이해가 깊어진다.
# ─────────────────────────────────────────────────────────────
```

</details>

*합집합(`|`)·교집합(`&`)·차집합(`-`)·대칭차집합(`^`) 네 연산자로 두 세트 사이의 관계를 한 번에 계산할 수 있다.*

**실행 결과**: `basic38.py`

```
{1, 2, 3, 4, 5, 6}
{3, 4}
{1, 2}
{1, 2, 5, 6}
```

---

**⑤ 세트 주요 메서드**

**예제 코드**: `basic39.py`

```python
# add()
fruits = {"apple", "banana"}
fruits.add("cherry")
print(fruits)   # 출력: {'apple', 'banana', 'cherry'}

# remove()
fruits.remove("banana")
print(fruits)   # 출력: {'apple', 'cherry'}

# discard() (없는 요소를 제거해도 오류가 발생하지 않음)
fruits.discard("grape")
print(fruits)   # 출력: {'apple', 'cherry'}

# pop()
removed_item = fruits.pop()
print(removed_item)   # 출력: apple (임의로 선택된 요소)
print(fruits)   # 출력: {'cherry'}

# clear()
fruits.clear()
print(fruits)   # 출력: set()
```

<details>
<summary><span class="label-badge">코드분석</span></summary>

```python
"""
2.2.3 세트 — 주요 메서드 5종(add/remove/discard/pop/clear)
"""

fruits = {"apple", "banana"}
fruits.add("cherry")        # 새 원소 추가(리스트의 append에 해당)
print(fruits)                # {'cherry', 'banana', 'apple'}

fruits.remove("banana")     # 값으로 제거 — 없는 값을 remove하면 KeyError
print(fruits)                # {'cherry', 'apple'}

fruits.discard("grape")     # remove와 달리 없는 값이어도 에러 없이 조용히 무시
print(fruits)                # {'cherry', 'apple'} (변화 없음)

removed_item = fruits.pop()  # 세트는 순서가 없어 "임의의" 원소 하나를 제거·반환
print(removed_item)          # 예: apple (어떤 값이 나올지 보장되지 않음)
print(fruits)                 # 제거되고 남은 나머지

fruits.clear()               # 모든 원소 제거 → 빈 세트
print(fruits)                 # set()

# ─────────────────────────────────────────────────────────────
# [교안용 설명 포인트]
# 1) remove()는 없는 값을 지우려 하면 KeyError를 내지만, discard()는
#    조용히 넘어간다는 차이를 꼭 비교해서 설명한다 — "안전하게 지우고
#    싶으면 discard, 반드시 존재를 확인하고 싶으면 remove."
# 2) 세트의 pop()은 리스트의 pop()과 달리 "어떤 원소가 나올지 알 수
#    없다" — 순서가 없기 때문이라는 점을 강조한다.
# 3) print(fruits)의 결과가 set()으로 표시되는 이유는, 빈 세트를
#    {}로 표기하면 빈 딕셔너리와 헷갈리기 때문이라는 것도 언급.
# ─────────────────────────────────────────────────────────────
```

</details>

*add/remove/discard/pop/clear로 세트 내용을 자유롭게 바꿀 수 있으며, discard는 없는 값을 지우려 해도 오류를 내지 않는다는 점이 remove와 다르다.*

**실행 결과**: `basic39.py`

```
{'cherry', 'banana', 'apple'}
{'cherry', 'apple'}
{'cherry', 'apple'}
cherry
{'apple'}
set()
```

---

**세트와 리스트의 차이점**

| 특징 | 세트 (Set) | 리스트 (List) |
|---|---|---|
| 순서 | 순서 없음 (Unordered) | 순서 있음 (Ordered) |
| 중복 여부 | 중복 요소 허용하지 않음 | 중복 요소 허용 |
| 변경 가능성 | 변경 가능 (Mutable) | 변경 가능 (Mutable) |
| 인덱스 사용 여부 | 인덱스를 통한 접근 불가능 | 인덱스를 통해 접근 가능 |
| 주요 사용 사례 | 집합 연산, 중복 제거 | 순서가 중요한 데이터 |

---

## 2.2.4 딕셔너리 (Dictionary)

- **정의**: 키-값 쌍(Key-Value Pair)을 저장하는 데이터 구조 — 다른 언어의 맵(Map)과 유사
- **특징**: 키-값 쌍 저장 · 순서 유지(3.7+) · 키는 중복 불허 · 변경 가능 · 키는 불변 자료형이어야 함
- **주요 메서드**: `keys() · values() · items() · get()` — 키/값 접근 및 안전한 조회 지원
- **활용 사례**: 사용자 정보 저장 · DB 레코드 표현 · 애플리케이션 설정값 저장

---

**① 딕셔너리 선언**

**예제 코드**: `basic40.py`

```python
person = {"name": "John", "age": 30, "city": "New York"}
print(person)   # 출력: {'name': 'John', 'age': 30, 'city': 'New York'}
```

<details>
<summary><span class="label-badge">코드분석</span></summary>

```python
"""
2.2.4 딕셔너리 — 중괄호 + 콜론으로 키-값 쌍 선언
"""

person = {"name": "John", "age": 30, "city": "New York"}
# "name","age","city"는 키(key), 그 뒤 값이 각각의 값(value)
print(person)

# ─────────────────────────────────────────────────────────────
# [교안용 설명 포인트]
# 1) 세트({"apple","banana"})와 딕셔너리({"name": "John"})는 둘 다
#    중괄호를 쓰지만, 콜론(:)이 있으면 딕셔너리, 없으면 세트라는
#    점으로 basic35와 비교해서 구분해준다.
# ─────────────────────────────────────────────────────────────
```

</details>

*"name", "age", "city"라는 세 개의 키에 각각의 값이 매핑된 딕셔너리가 선언 순서 그대로(Python 3.7+) 출력된다.*

**실행 결과**: `basic40.py`

```
{'name': 'John', 'age': 30, 'city': 'New York'}
```

---

**② 키-값 접근**

**예제 코드**: `basic41.py`

```python
person = {"name": "John", "age": 30, "city": "New York"}
print(person["name"])   # 출력: John
print(person["age"])    # 출력: 30
```

<details>
<summary><span class="label-badge">코드분석</span></summary>

```python
"""
2.2.4 딕셔너리 — 대괄호 + 키로 값 접근
"""

person = {"name": "John", "age": 30, "city": "New York"}

print(person["name"])   # 인덱스가 아니라 키 "name"으로 값 조회 → John
print(person["age"])    # 키 "age"로 값 조회 → 30

# ─────────────────────────────────────────────────────────────
# [교안용 설명 포인트]
# 1) person["address"]처럼 없는 키에 접근하면 KeyError가 발생한다는
#    것을 먼저 보여준 뒤, 다음 basic42의 in 확인, basic45의 get()으로
#    안전하게 조회하는 방법을 순서대로 연결해 설명하면 자연스럽다.
# ─────────────────────────────────────────────────────────────
```

</details>

*리스트가 정수 인덱스로 접근하는 것과 달리, 딕셔너리는 문자열(또는 다른 불변 값)인 키로 값을 조회한다.*

**실행 결과**: `basic41.py`

```
John
30
```

---

**③ 키 존재 여부 확인 (in)**

**예제 코드**: `basic42.py`

```python
person = {"name": "John", "age": 30, "city": "New York"}
print("name" in person)      # 출력: True
print("address" in person)   # 출력: False
```

<details>
<summary><span class="label-badge">코드분석</span></summary>

```python
"""
2.2.4 딕셔너리 — in으로 키 존재 여부 확인(KeyError 예방)
"""

person = {"name": "John", "age": 30, "city": "New York"}

print("name" in person)      # 딕셔너리에서 in은 "키" 목록을 기준으로 확인 → True
print("address" in person)   # "address"라는 키는 없음 → False

# ─────────────────────────────────────────────────────────────
# [교안용 설명 포인트]
# 1) 딕셔너리에서 in은 값(value)이 아니라 키(key) 기준으로 검사한다는
#    점이 리스트의 in(원소 기준)과 다르다 — "John" in person은 False가
#    된다는 것을 직접 보여주면 좋은 함정 문제가 된다.
# 2) 없는 키에 바로 접근(person["address"])하기 전에 in으로 먼저
#    확인하는 습관이 KeyError를 예방하는 안전한 패턴이라는 점을 강조.
# ─────────────────────────────────────────────────────────────
```

</details>

*딕셔너리에서 `in`은 값이 아니라 키를 기준으로 존재 여부를 확인한다. 없는 키에 바로 접근하기 전에 이렇게 먼저 확인하면 오류를 예방할 수 있다.*

**실행 결과**: `basic42.py`

```
True
False
```

---

**④ 값 변경 및 추가**

**예제 코드**: `basic43.py`

```python
person = {"name": "John", "age": 30, "city": "New York"}

# 값 변경
person["age"] = 35
print(person)   # 출력: {'name': 'John', 'age': 35, 'city': 'New York'}

# 새로운 항목 추가
person["email"] = "john@example.com"
print(person)   # 출력: {'name': 'John', 'age': 35, 'city': 'New York', 'email': 'john@example.com'}
```

<details>
<summary><span class="label-badge">코드분석</span></summary>

```python
"""
2.2.4 딕셔너리 — 값 변경과 새 항목 추가는 같은 문법

person[키] = 값 이라는 하나의 문법이, 키가 이미 있으면 "변경",
키가 없으면 "추가"로 동작한다.
"""

person = {"name": "John", "age": 30, "city": "New York"}

person["age"] = 35   # "age" 키가 이미 있으므로 값만 30 → 35로 변경
print(person)

person["email"] = "john@example.com"   # "email" 키가 없었으므로 새로 추가됨
print(person)

# ─────────────────────────────────────────────────────────────
# [교안용 설명 포인트]
# 1) 리스트는 append()라는 별도 메서드로 추가하지만, 딕셔너리는
#    "키가 있으면 수정, 없으면 추가"라는 동일한 대입 문법 하나로
#    두 가지를 모두 처리한다는 점이 핵심 차이다.
# ─────────────────────────────────────────────────────────────
```

</details>

*같은 `person[키] = 값` 문법이 키가 이미 있으면 값을 바꾸고, 없으면 새 키-값 쌍을 추가하는 두 가지 역할을 모두 한다.*

**실행 결과**: `basic43.py`

```
{'name': 'John', 'age': 35, 'city': 'New York'}
{'name': 'John', 'age': 35, 'city': 'New York', 'email': 'john@example.com'}
```

---

**⑤ 값 삭제 (del / pop)**

**예제 코드**: `basic44.py`

```python
person = {"name": "John", "age": 30, "city": "New York"}

# del 키워드 사용
del person["age"]
print(person)   # 출력: {'name': 'John', 'city': 'New York'}

# pop() 메서드 사용
city = person.pop("city")
print(person)   # 출력: {'name': 'John'}
print(city)      # 출력: New York
```

<details>
<summary><span class="label-badge">코드분석</span></summary>

```python
"""
2.2.4 딕셔너리 — del과 pop()으로 항목 삭제
"""

person = {"name": "John", "age": 30, "city": "New York"}

del person["age"]     # del은 키-값 쌍을 삭제만 할 뿐, 값을 돌려주지 않음
print(person)          # {'name': 'John', 'city': 'New York'}

city = person.pop("city")   # pop()은 삭제하면서 그 값을 반환(리스트 pop()과 유사)
print(person)                # {'name': 'John'}
print(city)                   # New York

# ─────────────────────────────────────────────────────────────
# [교안용 설명 포인트]
# 1) del person["age"]는 값을 버리고, city = person.pop("city")는
#    삭제와 동시에 값을 변수에 담을 수 있다는 차이를 비교해서
#    강조한다 — "삭제한 값을 나중에 써야 한다면 pop()을 쓴다."
# ─────────────────────────────────────────────────────────────
```

</details>

*`del`은 값을 돌려주지 않고 그냥 삭제하지만, `pop()`은 삭제하면서 그 값을 반환해 바로 변수에 담을 수 있다.*

**실행 결과**: `basic44.py`

```
{'name': 'John', 'city': 'New York'}
{'name': 'John'}
New York
```

---

**⑥ 딕셔너리 메서드 (keys/values/items/get)**

**예제 코드**: `basic45.py`

```python
person = {"name": "John", "age": 30, "city": "New York"}

# keys()
print(person.keys())     # 출력: dict_keys(['name', 'age', 'city'])
# values()
print(person.values())   # 출력: dict_values(['John', 30, 'New York'])
# items()
print(person.items())    # 출력: dict_items([('name', 'John'), ('age', 30), ('city', 'New York')])
# get()
print(person.get("name"))                     # 출력: John
print(person.get("address", "Not Found"))     # 출력: Not Found
```

<details>
<summary><span class="label-badge">코드분석</span></summary>

```python
"""
2.2.4 딕셔너리 — keys()/values()/items()/get()
"""

person = {"name": "John", "age": 30, "city": "New York"}

print(person.keys())     # 모든 키만 모아 dict_keys 뷰로 반환
print(person.values())   # 모든 값만 모아 dict_values 뷰로 반환
print(person.items())    # (키, 값) 튜플의 목록을 dict_items 뷰로 반환

print(person.get("name"))                 # 있는 키 → 해당 값 John
print(person.get("address", "Not Found")) # 없는 키 → KeyError 대신 기본값 "Not Found" 반환

# ─────────────────────────────────────────────────────────────
# [교안용 설명 포인트]
# 1) person["address"]는 KeyError를 내지만, person.get("address")는
#    None을(또는 지정한 기본값을) 조용히 돌려준다 — "안전하게 값을
#    꺼내고 싶을 때는 get()을 쓴다"는 실무 습관으로 정리해준다.
# 2) for key, value in person.items(): 형태로 items()를 for 문과
#    함께 쓰면 키와 값을 동시에 순회할 수 있다는 응용도 소개하면 좋다.
# ─────────────────────────────────────────────────────────────
```

</details>

*keys()/values()/items()로 딕셔너리의 구성 요소를 각각 조회할 수 있고, get()은 키가 없을 때 오류 대신 지정한 기본값을 돌려주는 안전한 조회 방법이다.*

**실행 결과**: `basic45.py`

```
dict_keys(['name', 'age', 'city'])
dict_values(['John', 30, 'New York'])
dict_items([('name', 'John'), ('age', 30), ('city', 'New York')])
John
Not Found
```

---

**딕셔너리와 리스트의 차이점**

| 특징 | 딕셔너리 (Dictionary) | 리스트 (List) |
|---|---|---|
| 순서 | 순서가 있음 (Python 3.7+) | 순서 있음 (Ordered) |
| 인덱싱 방법 | 키를 사용하여 인덱싱 | 인덱스를 사용하여 인덱싱 |
| 중복 여부 | 키는 중복을 허용하지 않음 | 중복 요소 허용 |
| 수정 가능성 | 값 수정 가능, 키는 수정 불가 | 수정·삭제·추가 모두 가능 |
| 주요 사용 사례 | 키-값 쌍을 다루는 경우 | 순서·중복이 중요한 경우 |

---

이상으로 2.2 자료구조(리스트·튜플·세트·딕셔너리)를 마친다. 다음 절인 2.3 함수에서는 이 절에서 다룬 자료구조를 매개변수와 반환값으로 주고받는 함수 작성법을 배운다.
