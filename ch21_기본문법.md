**2장 Python 기초 — 2.1 기본 문법**

# 2.1 기본 문법 (Basic Syntax)

변수 · 연산 · 조건문 · 반복문

*파이썬 인공지능 풀스택 · pp.46~91 중 2.1*

> Python은 중괄호 대신 들여쓰기로 코드 블록을 구분하고, 변수 선언 시 타입을 명시하지 않는 동적 타이핑 언어다. 이 절에서는 변수와 데이터 타입, 산술·비교·논리·대입·멤버십·식별 연산자, if/elif/else 조건문, for/while 반복문과 break/continue까지 Python 문법의 뼈대를 예제 코드와 실행 결과로 익힌다.

---

## 2.1.1 변수 (Variable)

- **정의**: 값을 저장하는 공간 — 타입 명시 없이 값 할당만으로 타입이 자동 결정(동적 타이핑)
- **명명 규칙**: 대소문자 구분, 첫 글자는 문자(숫자 불가), 공백 대신 `_`(밑줄) 사용, 예약어 사용 불가
- **주요 데이터 타입**: 정수(int) · 실수(float) · 문자열(str) · 불리언(bool)
- **타입 변환**: `int()`/`float()`/`str()` 등으로 변수의 타입을 명시적으로 변경 가능

---

**① 변수 선언과 사용**

정수·실수·문자열 세 가지 타입의 변수를 선언하고 값을 그대로 출력하는 가장 기본적인 예제다.

**예제 코드**: `basic01.py`

```python
# 변수 선언 및 값 할당
x = 5      # 정수형 변수
y = 3.14   # 실수형 변수
name = "Python"   # 문자열 변수

# 변수 값 출력
print(x)      # 출력: 5
print(y)      # 출력: 3.14
print(name)   # 출력: Python
```

<details>
<summary><span class="label-badge">코드분석</span></summary>

```python
"""
2.1.1 변수 — 변수 선언과 값 할당

타입을 명시하지 않고 값을 대입하는 것만으로 변수가 만들어지는 Python의
동적 타이핑을 보여주는 가장 기본 예제다.
"""

# ── 변수 선언 및 값 할당 ─────────────────────────────────────
x = 5          # 정수 리터럴 5를 대입 → x는 int 타입이 된다
y = 3.14       # 실수 리터럴 3.14를 대입 → y는 float 타입이 된다
name = "Python"  # 큰따옴표로 감싼 문자열 리터럴 → name은 str 타입이 된다
# 세 변수 모두 int x, float y = ... 처럼 타입을 앞에 적지 않는다 —
# 자바·C와 달리 "값의 타입"이 곧 "변수의 타입"을 결정한다

# ── 변수 값 출력 ─────────────────────────────────────────────
print(x)       # 5
print(y)       # 3.14
print(name)    # Python

# ─────────────────────────────────────────────────────────────
# [교안용 설명 포인트]
# 1) x, y, name처럼 변수명만으로는 타입을 알 수 없다는 점 — type(x)로
#    직접 확인해보는 basic02 예제로 자연스럽게 이어진다.
# 2) 변수명 관례(스네이크 케이스: name, my_variable)도 함께 짚어주면 좋다.
# ─────────────────────────────────────────────────────────────
```

</details>

*정수 5, 실수 3.14, 문자열 "Python"이 각각 변수에 담겨 그대로 출력된다. 변수 선언 시 타입을 적지 않아도 값에 따라 타입이 자동으로 정해지는 동적 타이핑을 보여준다.*

**실행 결과**: `basic01.py`

```
5
3.14
Python
```

---

**② 데이터 타입 (int/float/str/bool)**

`type()` 함수로 변수의 실제 데이터 타입을 직접 확인하는 예제다.

**예제 코드**: `basic02.py`

```python
a = 42          # int
b = 3.14159     # float
c = "Hello"     # str
d = True        # bool

# 각 데이터 타입 출력
print(type(a))  # 출력: <class 'int'>
print(type(b))  # 출력: <class 'float'>
print(type(c))  # 출력: <class 'str'>
print(type(d))  # 출력: <class 'bool'>
```

<details>
<summary><span class="label-badge">코드분석</span></summary>

```python
"""
2.1.1 변수 — type()으로 데이터 타입 확인

int, float, str, bool 네 가지 기본 타입을 각각 변수에 담고 type()으로
실제 타입을 조회한다.
"""

a = 42          # 정수 리터럴 → int
b = 3.14159     # 소수점이 있는 리터럴 → float
c = "Hello"     # 따옴표로 감싼 문자열 → str
d = True        # True/False 예약어 → bool (첫 글자가 대문자임에 주의)

# ── type() 확인 ──────────────────────────────────────────────
# type(값)은 그 값이 어떤 클래스(타입)의 인스턴스인지를 돌려준다
print(type(a))  # <class 'int'>
print(type(b))  # <class 'float'>
print(type(c))  # <class 'str'>
print(type(d))  # <class 'bool'>

# ─────────────────────────────────────────────────────────────
# [교안용 설명 포인트]
# 1) bool은 내부적으로 int의 하위 타입이라 True==1, False==0이 성립한다는
#    점을 궁금해하는 학습자가 많다 — 필요하면 print(True + 1) # 2 로 시연.
# 2) type(d)의 결과가 <class 'bool'>처럼 "class"로 표시되는 이유는
#    Python에서 모든 값(정수·문자열 포함)이 객체이기 때문이라는 점을
#    2.4 객체지향 절과 연결해 언급하면 좋다.
# ─────────────────────────────────────────────────────────────
```

</details>

*네 변수의 타입이 각각 int, float, str, bool로 출력된다. Python에서는 값 자체가 이미 객체이며, `type()`으로 언제든 그 클래스를 확인할 수 있다.*

**실행 결과**: `basic02.py`

```
<class 'int'>
<class 'float'>
<class 'str'>
<class 'bool'>
```

---

**③ 변수의 타입 변경**

`int()`, `float()`, `str()` 함수로 변수의 타입을 명시적으로 변환하는 예제다.

**예제 코드**: `basic03.py`

```python
# 정수 -> 실수로 변환
a = 10
b = float(a)   # b는 이제 실수형
print(b)       # 출력: 10.0

# 문자열 -> 정수로 변환
s = "123"
num = int(s)   # num은 이제 정수형
print(num)     # 출력: 123

# 정수에서 문자열로 변환
x = 100
str_x = str(x)
print(str_x)   # 출력: "100"

# 문자열에서 실수로 변환
y = "3.14"
float_y = float(y)
print(float_y) # 출력: 3.14
```

<details>
<summary><span class="label-badge">코드분석</span></summary>

```python
"""
2.1.1 변수 — 명시적 타입 변환(형 변환)

int()/float()/str()로 값을 다른 타입으로 강제 변환하는 네 가지 경우를
보여준다.
"""

# ── int -> float ─────────────────────────────────────────────
a = 10
b = float(a)     # 정수 10을 실수 10.0으로 변환
print(b)         # 10.0

# ── str -> int ───────────────────────────────────────────────
s = "123"
num = int(s)     # 숫자로만 이루어진 문자열은 int()로 정수 변환 가능
print(num)       # 123
# 주의: int("3.14")처럼 소수점이 섞이면 ValueError가 발생한다(실무 팁)

# ── int -> str ───────────────────────────────────────────────
x = 100
str_x = str(x)   # 숫자를 문자열로 변환 — print(x) + "회" 같은 문자열
                 # 결합(concatenation)을 하려면 반드시 이렇게 str로 바꿔야 함
print(str_x)     # 100

# ── str -> float ─────────────────────────────────────────────
y = "3.14"
float_y = float(y)  # 소수점 형태 문자열은 float()로 변환 가능
print(float_y)      # 3.14

# ─────────────────────────────────────────────────────────────
# [교안용 설명 포인트]
# 1) print(str_x)의 실제 출력은 따옴표 없는 100이다 — 원본 주석의
#    # 출력: "100" 표기는 "문자열 타입이 되었다"는 의미이지, 화면에
#    따옴표가 찍힌다는 뜻이 아니라는 점을 짚어줘야 오해가 없다.
# 2) int("3.14")나 int("abc")처럼 변환 불가능한 문자열을 넣으면
#    ValueError: invalid literal for int()가 발생한다는 것을 실습으로
#    보여주면 예외 처리(향후 학습)와 자연스럽게 연결된다.
# ─────────────────────────────────────────────────────────────
```

</details>

*정수↔실수↔문자열 사이의 명시적 변환 결과를 확인할 수 있다. `str_x`의 값 100은 이제 숫자가 아니라 문자열이므로, 이후 다른 문자열과 `+`로 이어붙일 수 있게 된다.*

**실행 결과**: `basic03.py`

```
10.0
123
100
3.14
```

---

## 2.1.2 연산 (Operators)

- **산술 연산자**: `+ - * / // % **` — 덧셈·뺄셈·곱셈·나눗셈·몫·나머지·거듭제곱
- **비교 연산자**: `== != > < >= <=` — 두 값을 비교하여 True/False 반환
- **논리 연산자**: `and · or · not` — 여러 조건을 결합하여 하나의 논리값 생성
- **대입/멤버십/식별 연산자**: `+=, -=` 등 축약형 대입 / `in, not in` / `is, is not`

---

**① 산술 연산자**

일곱 가지 산술 연산자(`+ - * / // % **`)를 하나씩 실행해보는 예제다.

**예제 코드**: `basic04.py`

```python
# 산술 연산자 예시
a = 10
b = 3

# 덧셈
print(a + b)   # 출력: 13
# 뺄셈
print(a - b)   # 출력: 7
# 곱셈
print(a * b)   # 출력: 30
# 나눗셈
print(a / b)   # 출력: 3.3333...
# 몫 (정수 나눗셈)
print(a // b)  # 출력: 3
# 나머지
print(a % b)   # 출력: 1
# 거듭제곱
print(a ** b)  # 출력: 1000
```

<details>
<summary><span class="label-badge">코드분석</span></summary>

```python
"""
2.1.2 연산 — 산술 연산자 7종
"""

a = 10
b = 3

print(a + b)    # 13  → 덧셈
print(a - b)    # 7   → 뺄셈
print(a * b)    # 30  → 곱셈
print(a / b)    # 3.3333333333333335 → 나눗셈(/)은 항상 float를 반환
print(a // b)   # 3   → 나눗셈(//)은 몫만 취하고 소수점 이하를 버림
print(a % b)    # 1   → 나머지(10 = 3*3 + 1)
print(a ** b)   # 1000 → 거듭제곱(10의 3제곱)

# ─────────────────────────────────────────────────────────────
# [교안용 설명 포인트]
# 1) a / b는 3.3333333333333335로, 원본 주석의 "3.3333..."은 축약
#    표기임을 짚어준다 — 실제 부동소수점 오차까지 눈으로 보여주면 좋다.
# 2) a // b와 a % b의 관계: a == (a // b) * b + (a % b) 공식으로
#    "몫과 나머지"의 개념을 한 번에 정리해줄 수 있다.
# ─────────────────────────────────────────────────────────────
```

</details>

*나눗셈(`/`)은 항상 실수를 반환하고, 몫 연산(`//`)은 소수점 이하를 버린 정수를 반환한다는 차이가 핵심이다.*

**실행 결과**: `basic04.py`

```
13
7
30
3.3333333333333335
3
1
1000
```

---

**② 비교 연산자**

여섯 가지 비교 연산자로 두 값을 비교해 True/False를 얻는 예제다.

**예제 코드**: `basic05.py`

```python
x = 5
y = 10

# 같음
print(x == y)   # 출력: False
# 다름
print(x != y)   # 출력: True
# 크다
print(x > y)    # 출력: False
# 작다
print(x < y)    # 출력: True
# 크거나 같음
print(x >= y)   # 출력: False
# 작거나 같음
print(x <= y)   # 출력: True
```

<details>
<summary><span class="label-badge">코드분석</span></summary>

```python
"""
2.1.2 연산 — 비교 연산자 6종
"""

x = 5
y = 10

print(x == y)   # False → 값이 같은지(등호 두 개, = 대입과 구분)
print(x != y)   # True  → 값이 다른지
print(x > y)    # False → x가 y보다 큰지
print(x < y)    # True  → x가 y보다 작은지
print(x >= y)   # False → x가 y보다 크거나 같은지
print(x <= y)   # True  → x가 y보다 작거나 같은지

# ─────────────────────────────────────────────────────────────
# [교안용 설명 포인트]
# 1) x == y(비교)와 x = y(대입)를 혼동하는 초보자가 많다는 점을 꼭
#    강조 — 조건문에서 if x = y:라고 쓰면 Python은 SyntaxError를 낸다.
# 2) 비교 연산의 결과는 항상 bool(True/False)이라는 점이 2.1.3 조건문의
#    if 조건식과 바로 연결된다.
# ─────────────────────────────────────────────────────────────
```

</details>

*비교 연산자의 결과는 항상 True/False의 불리언 값이며, 이 값은 다음 절의 if 조건식에 그대로 쓰인다.*

**실행 결과**: `basic05.py`

```
False
True
False
True
False
True
```

---

**③ 논리 연산자**

`and`, `or`, `not`으로 불리언 값을 조합하는 예제다.

**예제 코드**: `basic06.py`

```python
a = True
b = False

# and 연산자
print(a and b)   # 출력: False
# or 연산자
print(a or b)    # 출력: True
# not 연산자
print(not a)     # 출력: False
```

<details>
<summary><span class="label-badge">코드분석</span></summary>

```python
"""
2.1.2 연산 — 논리 연산자 3종
"""

a = True
b = False

print(a and b)   # False → 둘 다 True일 때만 True (True and False → False)
print(a or b)    # True  → 하나만 True여도 True (True or False → True)
print(not a)     # False → a(True)를 반대로 뒤집음

# ─────────────────────────────────────────────────────────────
# [교안용 설명 포인트]
# 1) and/or는 왼쪽 값이 이미 결과를 결정하면 오른쪽은 평가하지 않는
#    "단락 평가(short-circuit)" 특성이 있다 — 심화 학습자에게는
#    함수 호출을 조건에 섞을 때 이 특성이 성능·안전성에 영향을 준다고
#    짚어주면 좋다.
# 2) 2.1.3의 ④ 복합 조건(and/or/not) 예제로 바로 이어진다.
# ─────────────────────────────────────────────────────────────
```

</details>

*`and`는 둘 다 참일 때만, `or`는 하나만 참이어도 True를 반환하며, `not`은 값을 반대로 뒤집는다.*

**실행 결과**: `basic06.py`

```
False
True
False
```

---

> **[원본 자료 안내] basic07 결번**: 원본 교재는 "연산자(ch02/basic07.py)"라는 표기만 두고, 대입 연산자(`=`, `+=`, `-=` 등) 목록을 코드 없이 텍스트로만 제시한다. 실제 코드 파일이 존재하지 않아 이 교안에서도 basic07을 건너뛰고, 다음 예제인 basic08부터 이어서 다룬다.

---

**④ 대입 연산자**

`+=`, `-=`, `*=` 등 축약형 대입 연산자를 하나씩 적용해가는 예제다.

**예제 코드**: `basic08.py`

```python
x = 10

# 덧셈 후 대입
x += 5
print(x)   # 출력: 15
# 뺄셈 후 대입
x -= 3
print(x)   # 출력: 12
# 곱셈 후 대입
x *= 2
print(x)   # 출력: 24
# 나눗셈 후 대입
x /= 4
print(x)   # 출력: 6.0
# 몫 후 대입
x //= 2
print(x)   # 출력: 3.0
# 나머지 후 대입
x %= 2
print(x)   # 출력: 1.0
# 거듭제곱 후 대입
x **= 3
print(x)   # 출력: 1.0
```

<details>
<summary><span class="label-badge">코드분석</span></summary>

```python
"""
2.1.2 연산 — 축약형 대입 연산자

x = x + 5 와 x += 5는 같은 동작을 하지만, 후자가 더 짧고 널리 쓰인다.
"""

x = 10
x += 5    # x = x + 5  → 15
print(x)  # 15
x -= 3    # x = x - 3  → 12
print(x)  # 12
x *= 2    # x = x * 2  → 24
print(x)  # 24
x /= 4    # x = x / 4  → 6.0 (나눗셈이라 float로 바뀜에 주의)
print(x)  # 6.0
x //= 2   # x = x // 2 → 3.0 (float끼리 몫 연산을 해도 float 유지)
print(x)  # 3.0
x %= 2    # x = x % 2  → 1.0
print(x)  # 1.0
x **= 3   # x = x ** 3 → 1.0
print(x)  # 1.0

# ─────────────────────────────────────────────────────────────
# [교안용 설명 포인트]
# 1) x /= 4를 거치는 순간 x가 int(24)에서 float(6.0)로 바뀌고, 이후
#    //=, %=, **= 연산 결과가 전부 float로 유지된다는 점이 이 예제의
#    핵심 포인트다 — "정수로 시작해도 나눗셈을 거치면 실수가 된다."
# 2) x //= 2가 3.0(정수처럼 보이지만 float)인 이유를 //가 "타입을
#    바꾸지 않고 소수점만 버리는" 연산이기 때문이라고 설명하면 좋다.
# ─────────────────────────────────────────────────────────────
```

</details>

*`x /= 4`를 거치면서 정수였던 x가 float(6.0)로 바뀌고, 이후 몫·나머지·거듭제곱 결과가 모두 float으로 유지된다.*

**실행 결과**: `basic08.py`

```
15
12
24
6.0
3.0
1.0
1.0
```

---

**⑤ 멤버십 연산자 (in / not in)**

리스트 안에 특정 값이 포함되어 있는지 확인하는 예제다.

**예제 코드**: `basic09.py`

```python
# 리스트 내 포함 여부 확인
fruits = ["apple", "banana", "cherry"]
print("apple" in fruits)       # 출력: True
print("orange" not in fruits)  # 출력: True
```

<details>
<summary><span class="label-badge">코드분석</span></summary>

```python
"""
2.1.2 연산 — 멤버십 연산자 in / not in
"""

fruits = ["apple", "banana", "cherry"]

print("apple" in fruits)       # True  → "apple"이 리스트 안에 있음
print("orange" not in fruits)  # True  → "orange"는 리스트 안에 없음

# ─────────────────────────────────────────────────────────────
# [교안용 설명 포인트]
# 1) in/not in은 리스트뿐 아니라 문자열("py" in "python")·튜플·세트·
#    딕셔너리(키 기준)에도 동일하게 쓸 수 있다는 점을 2.2 자료구조
#    절과 연결해 언급하면 좋다.
# ─────────────────────────────────────────────────────────────
```

</details>

*`in`/`not in`은 값이 컬렉션 안에 존재하는지를 True/False로 알려주며, 리스트·문자열·딕셔너리 등 여러 자료형에 공통으로 쓰인다.*

**실행 결과**: `basic09.py`

```
True
True
```

---

**⑥ 식별 연산자 (is / is not)**

값이 같은지가 아니라 "같은 객체인지"를 확인하는 예제다.

**예제 코드**: `basic10.py`

```python
a = [1, 2, 3]
b = [1, 2, 3]

# 객체 비교
print(a is b)      # 출력: False (다른 객체이지만 값은 동일)
print(a is not b)  # 출력: True

# 같은 객체인지 확인
c = a
print(a is c)      # 출력: True (같은 객체를 참조)
```

<details>
<summary><span class="label-badge">코드분석</span></summary>

```python
"""
2.1.2 연산 — 식별 연산자 is / is not

is는 값(==)이 아니라 메모리 상의 "동일 객체 여부"를 비교한다.
"""

a = [1, 2, 3]
b = [1, 2, 3]   # a와 값은 같지만, 별도로 새로 만들어진 리스트 객체

print(a is b)       # False → 값은 같아도(a == b는 True) 서로 다른 객체
print(a is not b)   # True

c = a               # c는 a와 "같은" 리스트 객체를 가리킨다(복사가 아님)
print(a is c)       # True → 같은 객체를 참조하므로 True

# ─────────────────────────────────────────────────────────────
# [교안용 설명 포인트]
# 1) a == b(값 비교)와 a is b(객체 비교)의 차이를 명확히 구분해줘야
#    한다 — a == b는 True, a is b는 False가 된다는 것을 함께 보여주면
#    개념이 확실해진다.
# 2) c = a는 리스트를 "복사"하는 게 아니라 같은 리스트를 가리키는
#    이름을 하나 더 만드는 것 — c.append(4)를 하면 a도 함께 바뀐다는
#    점을 실습으로 보여주면 2.2.1 리스트의 가변성(mutability) 학습에
#    도움이 된다.
# ─────────────────────────────────────────────────────────────
```

</details>

*값이 같아도 서로 다른 객체이면 `is`는 False를 반환한다. `c = a`처럼 같은 객체를 가리키게 한 경우에만 `is`가 True가 된다.*

**실행 결과**: `basic10.py`

```
False
True
True
```

---

## 2.1.3 조건문 (Conditional Statements)

- **역할**: 주어진 조건에 따라 코드 실행 여부를 분기 — 프로그램에 다양한 실행 경로 부여
- **기본 구문**: `if / elif / else` — 조건이 참(True)/거짓(False)인지에 따라 분기
- **복합 조건**: `and, or, not` 논리 연산자로 여러 조건 결합
- **중첩 조건문**: if 블록 안에 또 다른 if 블록 — 복잡한 조건 처리에 유용

---

**① if 구문**

조건이 참일 때만 실행되는 가장 단순한 형태다.

**예제 코드**: `basic11.py`

```python
age = 20
if age >= 18:
    print("성인입니다.")   # 출력: 성인입니다.
```

<details>
<summary><span class="label-badge">코드분석</span></summary>

```python
"""
2.1.3 조건문 — if 단독 구문
"""

age = 20
if age >= 18:        # 조건식(age >= 18)이 True일 때만 아래 블록 실행
    print("성인입니다.")
# else가 없으므로 조건이 False였다면 아무 것도 출력되지 않는다

# ─────────────────────────────────────────────────────────────
# [교안용 설명 포인트]
# 1) if 블록은 콜론(:)과 들여쓰기로 구분된다 — 중괄호가 없는 대신
#    들여쓰기 자체가 문법이라는 점을 다시 한 번 강조한다.
# ─────────────────────────────────────────────────────────────
```

</details>

*나이가 18 이상이므로 조건이 참이 되어 "성인입니다."가 출력된다.*

**실행 결과**: `basic11.py`

```
성인입니다.
```

---

**② if-else 구문**

조건이 거짓일 때 실행할 코드를 `else`로 지정하는 예제다.

**예제 코드**: `basic12.py`

```python
age = 16
if age >= 18:
    print("성인입니다.")
else:
    print("미성년자입니다.")   # 출력: 미성년자입니다.
```

<details>
<summary><span class="label-badge">코드분석</span></summary>

```python
"""
2.1.3 조건문 — if-else
"""

age = 16
if age >= 18:              # 16 >= 18은 False
    print("성인입니다.")
else:                      # 조건이 False이므로 이 블록이 실행됨
    print("미성년자입니다.")

# ─────────────────────────────────────────────────────────────
# [교안용 설명 포인트]
# 1) if와 else는 반드시 한 쌍이며, if 조건이 False일 때 else 블록이
#    "무조건" 실행된다 — 둘 중 하나는 반드시 실행된다는 점을 강조.
# ─────────────────────────────────────────────────────────────
```

</details>

*나이가 18 미만이라 if 조건이 거짓이 되어 else 블록의 "미성년자입니다."가 출력된다.*

**실행 결과**: `basic12.py`

```
미성년자입니다.
```

---

**③ elif 구문**

세 갈래 이상으로 분기할 때 쓰는 `elif`(else if) 예제다.

**예제 코드**: `basic13.py`

```python
age = 20
if age < 18:
    print("미성년자입니다.")
elif age == 18:
    print("방금 성인이 되었습니다.")
else:
    print("성인입니다.")   # 출력: 성인입니다.
```

<details>
<summary><span class="label-badge">코드분석</span></summary>

```python
"""
2.1.3 조건문 — elif로 3갈래 이상 분기
"""

age = 20
if age < 18:                       # 20 < 18 → False, 다음 조건 확인
    print("미성년자입니다.")
elif age == 18:                    # 20 == 18 → False, 다음 조건 확인
    print("방금 성인이 되었습니다.")
else:                               # 위 두 조건이 모두 False → 실행
    print("성인입니다.")

# ─────────────────────────────────────────────────────────────
# [교안용 설명 포인트]
# 1) if/elif/elif/.../else는 위에서부터 순서대로 검사하다가 처음으로
#    True가 되는 블록 하나만 실행하고 나머지는 건너뛴다는 점(단락
#    실행)을 강조한다 — 조건이 여러 개 동시에 참이어도 마지막까지
#    다 확인하지 않는다.
# ─────────────────────────────────────────────────────────────
```

</details>

*age가 20이므로 첫 번째·두 번째 조건은 모두 거짓이 되고, else 블록의 "성인입니다."가 출력된다.*

**실행 결과**: `basic13.py`

```
성인입니다.
```

---

**④ 복합 조건 (and/or/not)**

논리 연산자로 여러 조건을 하나의 if 문에 결합하는 예제다.

**예제 코드**: `basic14.py`

```python
age = 20
citizenship = "Korea"

# and 예제
if age >= 18 and citizenship == "Korea":
    print("한국 성인입니다.")   # 출력: 한국 성인입니다.

# or 예제
has_id_card = True
if age >= 18 or has_id_card:
    print("성인으로 간주됩니다.")   # 출력: 성인으로 간주됩니다.

# not 예제
is_raining = False
if not is_raining:
    print("비가 오지 않습니다.")   # 출력: 비가 오지 않습니다.
```

<details>
<summary><span class="label-badge">코드분석</span></summary>

```python
"""
2.1.3 조건문 — and/or/not을 조건식에 결합
"""

age = 20
citizenship = "Korea"

# and: 두 조건이 모두 True여야 블록 실행
if age >= 18 and citizenship == "Korea":
    print("한국 성인입니다.")   # 20>=18(True) and "Korea"=="Korea"(True) → True

has_id_card = True
# or: 둘 중 하나만 True여도 블록 실행
if age >= 18 or has_id_card:
    print("성인으로 간주됩니다.")   # True or True → True (둘 다 True라도 무방)

is_raining = False
# not: 값을 반대로 뒤집어서 판단
if not is_raining:
    print("비가 오지 않습니다.")   # not False → True

# ─────────────────────────────────────────────────────────────
# [교안용 설명 포인트]
# 1) and는 두 조건 모두, or는 하나만 만족해도 되는 차이를 실제
#    조건문 안에서 다시 확인시켜준다 — 2.1.2의 ③ 논리 연산자 예제와
#    바로 이어지는 응용이다.
# 2) not is_raining처럼 "부정 조건"으로 이름을 지으면 코드가 더
#    읽기 쉬워진다는 스타일 팁을 함께 전달하면 좋다.
# ─────────────────────────────────────────────────────────────
```

</details>

*세 조건 모두 참이 되어 세 개의 print문이 순서대로 모두 실행된다.*

**실행 결과**: `basic14.py`

```
한국 성인입니다.
성인으로 간주됩니다.
비가 오지 않습니다.
```

---

**⑤ 중첩 조건문 (Nested if)**

if 블록 안에 또 다른 if 블록을 넣는 예제다.

**예제 코드**: `basic15.py`

```python
age = 20
is_student = True

if age >= 18:
    if is_student:
        print("성인 학생입니다.")   # 출력: 성인 학생입니다.
    else:
        print("성인입니다.")
else:
    print("미성년자입니다.")
```

<details>
<summary><span class="label-badge">코드분석</span></summary>

```python
"""
2.1.3 조건문 — if 안에 if를 중첩

바깥쪽 if로 큰 범주를 나누고, 그 안에서 다시 세부 조건을 나눈다.
"""

age = 20
is_student = True

if age >= 18:              # 바깥쪽 조건: 성인 여부 (20>=18 → True)
    if is_student:         # 안쪽 조건: 학생 여부 (True)
        print("성인 학생입니다.")
    else:
        print("성인입니다.")
else:
    print("미성년자입니다.")

# ─────────────────────────────────────────────────────────────
# [교안용 설명 포인트]
# 1) 들여쓰기 단계가 곧 "어느 if에 속하는지"를 나타낸다 — 안쪽 if의
#    들여쓰기가 바깥쪽 if 블록보다 한 단계 더 깊다는 것을 강조.
# 2) if age >= 18 and is_student: 처럼 and로 합쳐서 한 줄로 쓸 수도
#    있다는 것을 보여주면, "중첩"과 "복합 조건"의 관계를 이해하기
#    쉬워진다(단, else 분기가 다를 때는 중첩이 필요하다는 점도 짚기).
# ─────────────────────────────────────────────────────────────
```

</details>

*age가 18 이상이면서 동시에 학생이므로, 안쪽 if 블록의 "성인 학생입니다."가 출력된다.*

**실행 결과**: `basic15.py`

```
성인 학생입니다.
```

---

## 2.1.4 반복문 (Loops)

- **for 문**: 리스트·튜플·문자열 등 순차 자료형 또는 `range()`를 순회하며 반복
- **while 문**: 조건이 참인 동안 계속 반복 — 조건 설정을 잘못하면 무한 루프 위험
- **break / continue**: break는 반복 즉시 종료, continue는 현재 반복을 건너뛰고 다음으로 진행
- **중첩 반복문**: 반복문 안에 또 다른 반복문 — 2차원 이상의 데이터 처리에 유용

---

**① for 문 — 리스트 순회**

리스트의 각 원소를 차례로 꺼내 출력하는 가장 기본적인 for 문이다.

**예제 코드**: `basic16.py`

```python
fruits = ["apple", "banana", "cherry"]
for fruit in fruits:
    print(fruit)
```

<details>
<summary><span class="label-badge">코드분석</span></summary>

```python
"""
2.1.4 반복문 — for로 리스트 순회
"""

fruits = ["apple", "banana", "cherry"]
for fruit in fruits:   # fruits의 원소를 앞에서부터 하나씩 fruit에 대입
    print(fruit)        # apple → banana → cherry 순서로 3번 실행

# ─────────────────────────────────────────────────────────────
# [교안용 설명 포인트]
# 1) C/Java의 for(int i=0; i<n; i++) 스타일과 달리, Python의 for는
#    "각 원소를 순서대로 꺼내는" 반복이라는 점을 강조 — 인덱스가
#    필요 없다.
# ─────────────────────────────────────────────────────────────
```

</details>

*리스트의 원소 3개가 apple, banana, cherry 순서로 한 줄씩 출력된다.*

**실행 결과**: `basic16.py`

```
apple
banana
cherry
```

---

**② for 문 — range() 사용**

`range()`로 정해진 횟수만큼 반복하는 예제다.

**예제 코드**: `basic17.py`

```python
for i in range(1, 6):   # 1부터 5까지 반복
    print(i)
```

<details>
<summary><span class="label-badge">코드분석</span></summary>

```python
"""
2.1.4 반복문 — range()로 정해진 횟수 반복
"""

for i in range(1, 6):   # range(1, 6) → 1, 2, 3, 4, 5 (끝 값 6은 포함 안 됨)
    print(i)

# ─────────────────────────────────────────────────────────────
# [교안용 설명 포인트]
# 1) range(start, stop)은 stop을 포함하지 않는다는 점이 초보자가
#    가장 자주 틀리는 부분이다 — range(1, 6)이 5개(1~5)를 만든다는
#    것을 직접 세어보게 하면 좋다.
# 2) range(6)처럼 시작값을 생략하면 0부터 시작한다는 것도 함께 언급.
# ─────────────────────────────────────────────────────────────
```

</details>

*1부터 5까지 다섯 개의 정수가 한 줄씩 출력된다. `range(1, 6)`은 6을 포함하지 않는다.*

**실행 결과**: `basic17.py`

```
1
2
3
4
5
```

---

**③ while 문**

조건이 참인 동안 반복하는 `while` 문 예제다.

**예제 코드**: `basic18.py`

```python
count = 1
while count <= 5:
    print(count)
    count += 1   # count 값을 1씩 증가시킴
```

<details>
<summary><span class="label-badge">코드분석</span></summary>

```python
"""
2.1.4 반복문 — while 문
"""

count = 1
while count <= 5:    # count가 5 이하인 동안 계속 반복
    print(count)
    count += 1        # 반드시 조건에 영향을 주는 값을 갱신해야 함
                       # (이 줄이 없으면 무한 루프가 된다 — 다음 예제 참고)

# ─────────────────────────────────────────────────────────────
# [교안용 설명 포인트]
# 1) while의 조건은 매 반복마다 다시 검사된다 — count가 6이 되는
#    순간(6 <= 5는 False) 루프가 종료된다는 흐름을 강조.
# 2) count += 1을 빼먹으면 어떻게 되는지 바로 다음 basic19 예제로
#    확인시켜주는 흐름이 이 절의 핵심 학습 포인트다.
# ─────────────────────────────────────────────────────────────
```

</details>

*count가 1부터 5까지 증가하며 매번 출력되고, count가 6이 되는 순간 조건(`count <= 5`)이 거짓이 되어 반복이 끝난다.*

**실행 결과**: `basic18.py`

```
1
2
3
4
5
```

---

**④ while 문 — 무한 루프 예시**

조건이 항상 참(`True`)이라 스스로 끝나지 않는 무한 루프 예제다. 실제로 실행하면 영원히 종료되지 않으므로, 이 교안에서는 실행하지 않고 교재 원문의 설명을 그대로 인용한다.

**예제 코드**: `basic19.py`

```python
while True:
    print("이 메시지는 계속 출력됩니다.")
```

<details>
<summary><span class="label-badge">코드분석</span></summary>

```python
"""
2.1.4 반복문 — while True 무한 루프 (실행 주의)

조건식 자리에 상수 True를 직접 넣으면, 그 값은 절대 바뀌지 않으므로
루프를 멈출 방법이 코드 안에 없다.
"""

while True:                         # 조건이 항상 True → 절대 False가 되지 않음
    print("이 메시지는 계속 출력됩니다.")
# break, return, 강제 종료(Ctrl+C) 중 하나가 없으면 이 코드는 영원히 실행된다

# ─────────────────────────────────────────────────────────────
# [교안용 설명 포인트]
# 1) 이 예제는 실습 환경에서 절대 그대로 실행하지 말라고 먼저
#    경고해야 한다 — 실행하면 화면이 멈춘 것처럼 보이고, 강제
#    종료(Ctrl+C, 커널 재시작)로만 빠져나올 수 있다.
# 2) while True: 자체는 실무에서도 자주 쓰이는 패턴(서버의 요청
#    대기 루프 등)이며, 대신 내부에 반드시 break 조건을 두어야
#    "의도적으로 통제된" 무한 루프가 된다는 점을 다음 basic20의
#    break 예제와 연결해 설명하면 좋다.
# ─────────────────────────────────────────────────────────────
```

</details>

*교재가 의도적으로 제시한 무한 루프 예제로, 이 교안에서는 실제로 실행하지 않고 교재 원문의 설명을 그대로 인용한다. `while True:`처럼 조건이 항상 참이면 break나 강제 종료 없이는 반복이 끝나지 않는다는 것이 핵심이다.*

**실행 결과**: `basic19.py` (교재 원문 인용 — 실제 실행하지 않음)

```
(무한 루프 — 실행하지 않음)
이 메시지는 계속 출력됩니다.
이 메시지는 계속 출력됩니다.
이 메시지는 계속 출력됩니다.
...(영원히 반복)...
while True: 조건이 항상 True이므로 프로그램이 스스로 종료되지 않음.
강제 종료(Ctrl+C) 또는 break/조건 추가가 필요.
```

---

**⑤ break 사용**

특정 조건에서 반복을 즉시 종료하는 `break` 예제다.

**예제 코드**: `basic20.py`

```python
for i in range(1, 10):
    if i == 5:
        break   # i가 5일 때 반복을 종료
    print(i)
```

<details>
<summary><span class="label-badge">코드분석</span></summary>

```python
"""
2.1.4 반복문 — break로 반복 즉시 종료
"""

for i in range(1, 10):   # 1부터 9까지 반복할 예정이었지만
    if i == 5:
        break             # i가 5가 되는 순간 for 루프 전체를 즉시 빠져나감
    print(i)               # break 이전, 즉 1~4까지만 출력됨

# ─────────────────────────────────────────────────────────────
# [교안용 설명 포인트]
# 1) break는 "현재 반복문 하나만" 종료시킨다 — 중첩 반복문에서는
#    가장 안쪽 반복문만 빠져나간다는 점을 2.1.4 ⑦ 예제와 연결해
#    짚어주면 좋다.
# 2) print(i)가 break보다 뒤에 있었다면 5도 출력됐을지 확인해보게
#    하면(정답: break가 먼저 실행되어 5는 출력되지 않음) 실행 순서
#    이해에 도움이 된다.
# ─────────────────────────────────────────────────────────────
```

</details>

*i가 5가 되는 순간 `break`가 실행되어 반복이 즉시 끝나므로, 1부터 4까지만 출력된다.*

**실행 결과**: `basic20.py`

```
1
2
3
4
```

---

**⑥ continue 사용**

특정 조건에서 그 반복만 건너뛰는 `continue` 예제다.

**예제 코드**: `basic21.py`

```python
for i in range(1, 6):
    if i == 3:
        continue   # i가 3일 때는 출력하지 않고, 다음 반복으로 넘어감
    print(i)
```

<details>
<summary><span class="label-badge">코드분석</span></summary>

```python
"""
2.1.4 반복문 — continue로 특정 반복만 건너뛰기
"""

for i in range(1, 6):     # 1, 2, 3, 4, 5
    if i == 3:
        continue           # i==3일 때만 아래 print(i)를 건너뛰고 다음 반복으로
    print(i)                # 1, 2, 4, 5만 출력됨 (3은 건너뜀)

# ─────────────────────────────────────────────────────────────
# [교안용 설명 포인트]
# 1) break는 반복문을 "완전히 끝내지만", continue는 "이번 한 번만
#    건너뛰고 반복은 계속된다"는 차이를 basic20과 나란히 비교해
#    설명하면 가장 효과적이다.
# ─────────────────────────────────────────────────────────────
```

</details>

*i가 3일 때만 `continue`가 실행되어 그 반복의 `print(i)`가 건너뛰어지고, 나머지 1, 2, 4, 5는 정상적으로 출력된다.*

**실행 결과**: `basic21.py`

```
1
2
4
5
```

---

**⑦ 중첩 반복문 (Nested for)**

for 문 안에 또 다른 for 문을 넣어 2중 반복을 만드는 예제다.

**예제 코드**: `basic22.py`

```python
for i in range(1, 4):
    for j in range(1, 4):
        print(f"i = {i}, j = {j}")
```

<details>
<summary><span class="label-badge">코드분석</span></summary>

```python
"""
2.1.4 반복문 — 중첩 for문(이중 반복)

바깥쪽 루프가 한 번 돌 때마다 안쪽 루프가 처음부터 끝까지 전부 실행된다.
"""

for i in range(1, 4):          # 바깥쪽: i = 1, 2, 3 (총 3번)
    for j in range(1, 4):      # 안쪽: i가 고정된 채 j = 1, 2, 3 (매번 3번)
        print(f"i = {i}, j = {j}")   # f-string으로 변수를 문자열에 바로 삽입
# 전체 실행 횟수 = 바깥쪽 3번 × 안쪽 3번 = 총 9번

# ─────────────────────────────────────────────────────────────
# [교안용 설명 포인트]
# 1) 바깥쪽 i가 1로 고정된 상태에서 j가 1→2→3까지 다 돈 다음에야
#    비로소 i가 2로 넘어간다는 실행 순서를 표로 그려서 보여주면
#    중첩 반복문의 동작이 훨씬 잘 이해된다.
# 2) f"i = {i}, j = {j}"처럼 문자열 앞에 f를 붙이는 f-string은
#    "{변수}" 자리에 변수 값을 바로 넣어주는 최신 문자열 포매팅
#    방식이라는 것을 짚어준다.
# ─────────────────────────────────────────────────────────────
```

</details>

*바깥쪽 i가 1로 고정된 동안 안쪽 j가 1, 2, 3을 모두 돌고 나서야 i가 2로 넘어가는 방식으로, 총 3×3=9개의 조합이 출력된다.*

**실행 결과**: `basic22.py`

```
i = 1, j = 1
i = 1, j = 2
i = 1, j = 3
i = 2, j = 1
i = 2, j = 2
i = 2, j = 3
i = 3, j = 1
i = 3, j = 2
i = 3, j = 3
```

---

이상으로 2.1 기본 문법(변수·연산·조건문·반복문)을 마친다. 다음 절인 2.2 자료구조에서는 이번 절에서 다룬 값들을 리스트·튜플·세트·딕셔너리에 담아 다루는 방법을 배운다.
