**2장 Python 기초 — 2.3 함수**

# 2.3 함수 (Functions)

함수 정의와 호출 · 람다 함수

> 함수는 반복되는 코드를 하나의 이름으로 묶어 재사용하는 Python의 핵심 구조다. `def`로 정의한 일반 함수는 매개변수와 기본값, 여러 개의 반환값, 위치·키워드·가변 인자를 자유롭게 다룰 수 있고, `lambda`로 정의한 람다 함수는 이름 없이 한 줄짜리 간단한 계산을 즉석에서 만들 때 쓴다. 이 절에서는 함수 정의·호출의 기본형부터 `map()`·`filter()`·`sorted()`와 결합한 람다 활용까지 다룬다.

---

## 2.3.1 함수 정의와 호출

- **함수 정의**: `def 함수명(매개변수, ...):` — 이어서 실행 코드와 `return` 반환값 작성
- **함수 호출**: `함수명(인자, ...)` 형태로 호출 — 매개변수가 있으면 인자 값을 전달
- **기본 동작 종류**: 인자·반환값 없는 함수 / 매개변수가 있는 함수 / 기본값 매개변수 / 여러 값 반환(튜플)
- **호출 방식**: 위치 인자 · 키워드 인자 · 가변 인자(`*args, **kwargs`)

---

**① 함수 정의와 호출**

**예제 코드**: `basic46.py`

```python
# 함수 정의
def greet(name):
    return "Hello, " + name + "!"

# 함수 호출
result = greet("Alice")
print(result)   # 출력: Hello, Alice!
```

<details>
<summary><span class="label-badge">코드분석</span></summary>

```python
"""
2.3.1 함수 — def로 정의하고 return으로 값을 돌려주는 기본형
"""

def greet(name):              # greet라는 이름의 함수, 매개변수는 name 하나
    return "Hello, " + name + "!"   # 문자열을 이어붙여(concatenation) 반환

result = greet("Alice")       # "Alice"를 인자로 전달해 함수 호출, 반환값을 result에 저장
print(result)                  # Hello, Alice!

# ─────────────────────────────────────────────────────────────
# [교안용 설명 포인트]
# 1) 함수는 "정의"하는 것과 "호출"하는 것이 별개의 단계라는 점을
#    강조한다 — def greet(name): 줄만 실행해서는 아무 일도 일어나지
#    않고, greet("Alice")처럼 호출해야 비로소 코드가 실행된다.
# 2) return이 있는 함수는 결과를 변수에 담아 재사용할 수 있다는 것을
#    print(result)로 보여준다.
# ─────────────────────────────────────────────────────────────
```

</details>

*`greet` 함수를 "Alice"라는 인자로 호출하면 매개변수 name에 "Alice"가 대입되고, return이 만든 문자열이 result에 저장된 뒤 출력된다.*

**실행 결과**: `basic46.py`

```
Hello, Alice!
```

---

**② 인자·반환값 없는 함수**

**예제 코드**: `basic47.py`

```python
def print_hello():
    print("Hello, World!")

# 함수 호출
print_hello()   # 출력: Hello, World!
```

<details>
<summary><span class="label-badge">코드분석</span></summary>

```python
"""
2.3.1 함수 — 매개변수도 return도 없는 가장 단순한 형태
"""

def print_hello():        # 괄호 안에 매개변수가 없음 → 인자를 받지 않는 함수
    print("Hello, World!")  # return이 없으므로 값을 돌려주지 않고, 화면 출력만 함

print_hello()               # 괄호를 비운 채로 호출

# ─────────────────────────────────────────────────────────────
# [교안용 설명 포인트]
# 1) return이 없는 함수를 호출해 print(print_hello())처럼 결과를
#    출력해보면 None이 찍힌다는 것을 보여주면, "print()는 화면에
#    보여줄 뿐 값을 반환하지 않는다"는 점이 명확해진다.
# ─────────────────────────────────────────────────────────────
```

</details>

*매개변수도 반환값도 없는 가장 단순한 함수 형태로, 호출하면 내부의 print문이 그대로 실행될 뿐 값을 돌려주지는 않는다.*

**실행 결과**: `basic47.py`

```
Hello, World!
```

---

**③ 매개변수가 있는 함수**

**예제 코드**: `basic48.py`

```python
def add_numbers(a, b):
    return a + b

# 함수 호출
result = add_numbers(3, 4)
print(result)   # 출력: 7
```

<details>
<summary><span class="label-badge">코드분석</span></summary>

```python
"""
2.3.1 함수 — 매개변수 2개를 받아 계산 결과를 반환
"""

def add_numbers(a, b):   # a, b 두 개의 매개변수
    return a + b          # 두 값을 더한 결과를 반환

result = add_numbers(3, 4)   # 3은 a에, 4는 b에 순서대로 전달됨(위치 인자)
print(result)                  # 7

# ─────────────────────────────────────────────────────────────
# [교안용 설명 포인트]
# 1) add_numbers(3, 4)에서 첫 번째 값이 a, 두 번째 값이 b로 순서대로
#    대응된다는 것 — 이것이 바로 ⑥ 위치 인자의 개념임을 미리 예고한다.
# ─────────────────────────────────────────────────────────────
```

</details>

*3과 4가 각각 a, b에 순서대로 전달되어 더해진 7이 반환된다.*

**실행 결과**: `basic48.py`

```
7
```

---

**④ 기본값이 있는 매개변수**

**예제 코드**: `basic49.py`

```python
def greet(name="Guest"):
    return "Hello, " + name + "!"

# 기본값 사용
print(greet())          # 출력: Hello, Guest!
# 인자를 전달하여 사용
print(greet("Alice"))   # 출력: Hello, Alice!
```

<details>
<summary><span class="label-badge">코드분석</span></summary>

```python
"""
2.3.1 함수 — 기본값 매개변수(default parameter)
"""

def greet(name="Guest"):   # name에 기본값 "Guest"를 지정
    return "Hello, " + name + "!"

print(greet())          # 인자를 안 주면 기본값 "Guest"가 쓰임 → Hello, Guest!
print(greet("Alice"))   # 인자를 주면 기본값 대신 그 값이 쓰임 → Hello, Alice!

# ─────────────────────────────────────────────────────────────
# [교안용 설명 포인트]
# 1) 기본값 매개변수는 "인자를 생략해도 되는" 함수를 만드는 방법이다
#    — 함수 설계 시 자주 쓰이지 않는 옵션값에 기본값을 주는 습관을
#    소개하면 좋다.
# 2) 기본값이 있는 매개변수는 반드시 기본값이 없는 매개변수 뒤에
#    와야 한다는 문법 규칙(def f(a, b=1)은 되지만 def f(a=1, b)는
#    SyntaxError)도 함께 짚어주면 좋다.
# ─────────────────────────────────────────────────────────────
```

</details>

*인자를 생략하면 매개변수에 지정된 기본값 "Guest"가 쓰이고, 인자를 전달하면 그 값이 기본값을 대신한다.*

**실행 결과**: `basic49.py`

```
Hello, Guest!
Hello, Alice!
```

---

**⑤ 여러 개의 반환값**

**예제 코드**: `basic50.py`

```python
def calculate(x, y):
    sum_result = x + y
    diff_result = x - y
    return sum_result, diff_result

# 함수 호출
sum_val, diff_val = calculate(5, 3)
print(sum_val)    # 출력: 8
print(diff_val)   # 출력: 2
```

<details>
<summary><span class="label-badge">코드분석</span></summary>

```python
"""
2.3.1 함수 — return에 값을 여러 개 나열하면 튜플로 묶여 반환됨
"""

def calculate(x, y):
    sum_result = x + y     # 5 + 3 = 8
    diff_result = x - y    # 5 - 3 = 2
    return sum_result, diff_result   # (8, 2) 형태의 튜플을 반환

sum_val, diff_val = calculate(5, 3)   # 튜플 (8, 2)를 두 변수에 각각 풀어서 대입
print(sum_val)     # 8
print(diff_val)    # 2

# ─────────────────────────────────────────────────────────────
# [교안용 설명 포인트]
# 1) return a, b는 실제로는 return (a, b)와 같아서 튜플 하나를
#    반환하는 것이다 — result = calculate(5, 3) 한 변수로 받으면
#    result가 튜플 (8, 2) 전체가 된다는 것을 보여주면 이해가 깊어진다.
# 2) sum_val, diff_val = ... 처럼 여러 변수에 한 번에 값을 나눠 담는
#    것을 "튜플 언패킹(unpacking)"이라 부른다는 용어도 소개하면 좋다.
# ─────────────────────────────────────────────────────────────
```

</details>

*`return sum_result, diff_result`는 두 값을 튜플 `(8, 2)`로 묶어 반환하며, 호출부에서 `sum_val, diff_val = ...`로 각 변수에 나누어 담는다.*

**실행 결과**: `basic50.py`

```
8
2
```

---

**⑥ 위치 인자 (Positional Arguments)**

**예제 코드**: `basic51.py`

```python
def multiply(a, b):
    return a * b

result = multiply(3, 4)   # 순서대로 값 전달
print(result)   # 출력: 12
```

<details>
<summary><span class="label-badge">코드분석</span></summary>

```python
"""
2.3.1 함수 — 위치 인자(전달한 순서대로 매개변수에 대응)
"""

def multiply(a, b):
    return a * b

result = multiply(3, 4)   # 3은 첫 번째 매개변수 a, 4는 두 번째 매개변수 b
print(result)               # 3 * 4 = 12

# ─────────────────────────────────────────────────────────────
# [교안용 설명 포인트]
# 1) multiply(4, 3)으로 순서를 바꾸면 결과가 어떻게 달라지는지
#    직접 실행해보게 한다(곱셈은 결과가 같지만, 나눗셈 divide(a, b)
#    였다면 순서가 결과를 바꾼다는 점을 함께 짚으면 좋다).
# ─────────────────────────────────────────────────────────────
```

</details>

*인자를 전달한 순서 그대로 매개변수에 대응되는 방식을 위치 인자라고 부른다.*

**실행 결과**: `basic51.py`

```
12
```

---

**⑦ 키워드 인자 (Keyword Arguments)**

**예제 코드**: `basic52.py`

```python
def introduce(name, age):
    print(f"My name is {name} and I am {age} years old.")

introduce(age=25, name="Alice")   # 키워드 인자 방식
```

<details>
<summary><span class="label-badge">코드분석</span></summary>

```python
"""
2.3.1 함수 — 키워드 인자(매개변수 이름을 지정해 전달, 순서 무관)
"""

def introduce(name, age):
    print(f"My name is {name} and I am {age} years old.")

# age=25, name="Alice"처럼 "매개변수명=값" 형태로 전달하면
# 함수 정의의 순서(name, age)와 달라도 정확히 매칭된다
introduce(age=25, name="Alice")

# ─────────────────────────────────────────────────────────────
# [교안용 설명 포인트]
# 1) introduce("Alice", 25)(위치 인자)와 introduce(age=25,
#    name="Alice")(키워드 인자)가 같은 결과를 낸다는 것을 나란히
#    비교해 보여주면 차이를 이해하기 쉽다.
# 2) 매개변수가 많은 함수를 호출할 때 키워드 인자를 쓰면 "어떤 값이
#    무엇을 의미하는지" 코드만 보고도 알 수 있어 가독성이 좋아진다는
#    실무 이점도 언급하면 좋다.
# ─────────────────────────────────────────────────────────────
```

</details>

*`age=25, name="Alice"`처럼 매개변수 이름을 직접 지정해 전달하면, 함수 정의의 순서와 달라도 이름을 기준으로 정확히 매칭된다.*

**실행 결과**: `basic52.py`

```
My name is Alice and I am 25 years old.
```

---

**⑧ 가변 인자 (`*args`, `**kwargs`)**

**예제 코드**: `basic53.py`

```python
def sum_all(*args):
    return sum(args)

print(sum_all(1, 2, 3))   # 출력: 6

def print_details(**kwargs):
    for key, value in kwargs.items():
        print(f"{key}: {value}")

print_details(name="Alice", age=25, city="New York")
```

<details>
<summary><span class="label-badge">코드분석</span></summary>

```python
"""
2.3.1 함수 — 가변 인자 *args(위치 인자를 몇 개든), **kwargs(키워드
인자를 몇 개든)

몇 개의 인자가 들어올지 미리 정할 수 없을 때 쓰는 문법이다.
"""

def sum_all(*args):        # *args: 위치 인자를 몇 개를 넘기든 하나의 튜플로 묶어 받음
    return sum(args)        # args는 (1, 2, 3) — 내장 함수 sum()으로 합계 계산

print(sum_all(1, 2, 3))     # 6

def print_details(**kwargs):   # **kwargs: 키워드 인자를 몇 개를 넘기든 딕셔너리로 묶어 받음
    for key, value in kwargs.items():   # kwargs는 {"name": "Alice", "age": 25, "city": "New York"}
        print(f"{key}: {value}")

print_details(name="Alice", age=25, city="New York")

# ─────────────────────────────────────────────────────────────
# [교안용 설명 포인트]
# 1) *args 안의 args는 함수 안에서 "튜플"이고, **kwargs 안의
#    kwargs는 "딕셔너리"라는 점을 명확히 구분해준다 — 이름 자체는
#    관례일 뿐 *와 **가 진짜 문법이라는 점(*나머지, **옵션 처럼
#    다른 이름을 써도 동작함)도 언급하면 좋다.
# 2) sum_all(1, 2, 3, 4, 5)처럼 인자 개수를 바꿔가며 호출해보면
#    "개수가 정해지지 않은 인자"를 받는다는 의미가 체감된다.
# ─────────────────────────────────────────────────────────────
```

</details>

*`*args`는 개수가 정해지지 않은 위치 인자를 튜플로, `**kwargs`는 개수가 정해지지 않은 키워드 인자를 딕셔너리로 묶어서 함수 안으로 전달한다.*

**실행 결과**: `basic53.py`

```
6
name: Alice
age: 25
city: New York
```

---

## 2.3.2 람다 함수 (Lambda Functions)

- **정의**: 익명 함수(Anonymous Function) — `def` 없이 `lambda` 키워드로 정의하는 간단한 함수
- **문법**: `lambda 매개변수1, 매개변수2, ... : 반환값` — 단일 표현식만 평가하여 반환
- **활용**: 간단한 일회성 연산에 유용 — `map() · filter() · sorted()`의 key와 결합해 자주 사용
- **조건문 결합**: 삼항식 형태(`A if 조건 else B`)를 lambda 안에 사용해 조건부 로직 처리 가능

---

**① 간단한 덧셈 람다 함수**

**예제 코드**: `basic54.py`

```python
add = lambda x, y: x + y
print(add(3, 5))   # 출력: 8
```

<details>
<summary><span class="label-badge">코드분석</span></summary>

```python
"""
2.3.2 람다 함수 — lambda로 이름 없는 한 줄짜리 함수 만들기

def add(x, y): return x + y 와 완전히 동일한 동작을, 이름 없이
한 줄로 표현한 것이 lambda다.
"""

add = lambda x, y: x + y   # 매개변수 x, y / 콜론 뒤가 곧 반환값(return 없이)
print(add(3, 5))            # 함수처럼 그대로 호출 가능 → 8

# ─────────────────────────────────────────────────────────────
# [교안용 설명 포인트]
# 1) add = lambda x, y: x + y 를 def add(x, y): return x + y 와
#    나란히 놓고 비교해, "람다는 def+return을 한 줄로 압축한 것"
#    이라는 감각을 심어준다.
# 2) 람다는 표현식(expression) 하나만 담을 수 있어 if문·for문 같은
#    "문장(statement)"은 들어갈 수 없다는 제약도 짚어준다.
# ─────────────────────────────────────────────────────────────
```

</details>

*`lambda x, y: x + y`는 `def`와 `return` 없이 매개변수와 반환식만으로 함수를 즉석에서 만든다.*

**실행 결과**: `basic54.py`

```
8
```

---

**② map()과 람다 — 리스트 제곱값**

**예제 코드**: `basic55.py`

```python
numbers = [1, 2, 3, 4, 5]
squares = list(map(lambda x: x**2, numbers))
print(squares)   # 출력: [1, 4, 9, 16, 25]
```

<details>
<summary><span class="label-badge">코드분석</span></summary>

```python
"""
2.3.2 람다 함수 — map()에 람다를 넘겨 리스트의 모든 원소를 한 번에 변환
"""

numbers = [1, 2, 3, 4, 5]
# map(함수, 리스트): 리스트의 각 원소에 함수를 하나씩 적용한 결과를 돌려줌
# lambda x: x**2 는 "값을 하나 받아 제곱해서 돌려주는" 함수
squares = list(map(lambda x: x**2, numbers))   # map 결과는 그 자체로는 리스트가 아니라
                                                  # map 객체이므로 list()로 감싸 리스트로 변환
print(squares)   # [1, 4, 9, 16, 25]

# ─────────────────────────────────────────────────────────────
# [교안용 설명 포인트]
# 1) [x**2 for x in numbers] 리스트 컴프리헨션으로도 같은 결과를
#    만들 수 있다는 것을 비교해 보여주면, map+lambda가 "리스트의
#    모든 원소를 같은 규칙으로 바꾼다"는 감각이 뚜렷해진다.
# 2) list(map(...))처럼 map()의 결과를 반드시 list()로 감싸야
#    화면에서 리스트 형태로 보인다는 점(그냥 print(map(...))을
#    하면 <map object at ...>가 출력됨)을 실습으로 보여주면 좋다.
# ─────────────────────────────────────────────────────────────
```

</details>

*`map()`은 리스트의 모든 원소에 람다 함수를 하나씩 적용해 새로운 값들을 만들고, `list()`로 감싸야 눈에 보이는 리스트가 된다.*

**실행 결과**: `basic55.py`

```
[1, 4, 9, 16, 25]
```

---

**③ sorted()와 람다 — 키 기준 정렬**

**예제 코드**: `basic56.py`

```python
tuples = [(1, 'one'), (3, 'three'), (2, 'two')]
sorted_tuples = sorted(tuples, key=lambda x: x[0])
print(sorted_tuples)   # 출력: [(1, 'one'), (2, 'two'), (3, 'three')]
```

<details>
<summary><span class="label-badge">코드분석</span></summary>

```python
"""
2.3.2 람다 함수 — sorted()의 key 인자에 람다를 넘겨 정렬 기준 지정
"""

tuples = [(1, 'one'), (3, 'three'), (2, 'two')]
# key=lambda x: x[0] → 각 튜플 x에서 x[0](숫자 부분)만 뽑아 그 값을 기준으로 정렬
sorted_tuples = sorted(tuples, key=lambda x: x[0])
print(sorted_tuples)   # [(1, 'one'), (2, 'two'), (3, 'three')]

# ─────────────────────────────────────────────────────────────
# [교안용 설명 포인트]
# 1) sorted()는 원본 리스트(tuples)는 그대로 두고 정렬된 "새 리스트"를
#    반환한다 — 2.2.1의 sort()(제자리 정렬)와의 차이를 다시 상기시켜준다.
# 2) key=lambda x: x[1]로 바꾸면 문자열(one/two/three) 알파벳 순으로
#    정렬된다는 것을 실습으로 보여주면, "key는 정렬 기준을 뽑아내는
#    함수"라는 개념이 명확해진다.
# ─────────────────────────────────────────────────────────────
```

</details>

*`key=lambda x: x[0]`은 정렬 기준으로 각 튜플의 첫 번째 값(숫자)만 뽑아 쓰라는 뜻이며, 그 결과 숫자 순서대로 정렬된 새 리스트가 만들어진다.*

**실행 결과**: `basic56.py`

```
[(1, 'one'), (2, 'two'), (3, 'three')]
```

---

**④ 조건문과 함께 사용하는 람다**

**예제 코드**: `basic57.py`

```python
is_even = lambda x: True if x % 2 == 0 else False
print(is_even(4))   # 출력: True
print(is_even(5))   # 출력: False
```

<details>
<summary><span class="label-badge">코드분석</span></summary>

```python
"""
2.3.2 람다 함수 — 삼항식(조건부 표현식)을 람다 안에 사용
"""

# "True if 조건 else False" 는 "조건이 참이면 True, 아니면 False"라는
# 한 줄짜리 조건 표현식(삼항 연산자)이다
is_even = lambda x: True if x % 2 == 0 else False

print(is_even(4))   # 4 % 2 == 0 → True
print(is_even(5))   # 5 % 2 == 0 → False → else 분기 → False

# ─────────────────────────────────────────────────────────────
# [교안용 설명 포인트]
# 1) lambda x: x % 2 == 0 처럼 써도 결과는 동일하다(비교 연산 자체가
#    이미 True/False를 반환하므로 True if ... else False가 사실은
#    불필요하다) — "더 간단하게 쓸 수 있는 코드"를 찾아보는 실습으로
#    연결하면 좋은 심화 포인트가 된다.
# ─────────────────────────────────────────────────────────────
```

</details>

*`True if x % 2 == 0 else False` 형태의 조건부 표현식을 람다 안에 넣어, 짝수 여부를 한 줄로 판정한다.*

**실행 결과**: `basic57.py`

```
True
False
```

---

**람다 함수와 def 함수의 차이점**

| 특징 | 람다 함수 (Lambda) | 일반 함수 (Def) |
|---|---|---|
| 정의 방식 | lambda 키워드로 간단히 정의 | def 키워드를 사용하여 정의 |
| 이름 | 이름이 없는 익명 함수 | 이름이 있는 함수 |
| 내용 | 한 줄의 식만 포함 가능 | 여러 줄의 코드 블록 포함 가능 |
| 반환값 | 자동으로 평가된 값 반환 | return 문을 통해 반환 |
| 사용 용도 | 간단한 연산·일회성 작업 | 복잡하고 구조적인 작업 |

---

**⑤ filter()와 람다 — 짝수 걸러내기**

**예제 코드**: `basic58.py`

```python
numbers = [1, 2, 3, 4, 5, 6]
even_numbers = list(filter(lambda x: x % 2 == 0, numbers))
print(even_numbers)   # 출력: [2, 4, 6]
```

<details>
<summary><span class="label-badge">코드분석</span></summary>

```python
"""
2.3.2 람다 함수 — filter()에 람다를 넘겨 조건을 만족하는 원소만 추출
"""

numbers = [1, 2, 3, 4, 5, 6]
# filter(함수, 리스트): 함수가 True를 반환하는 원소만 남김
# lambda x: x % 2 == 0 는 "짝수인지" 판정하는 함수
even_numbers = list(filter(lambda x: x % 2 == 0, numbers))
print(even_numbers)   # [2, 4, 6]

# ─────────────────────────────────────────────────────────────
# [교안용 설명 포인트]
# 1) map()은 "모든 원소를 변환"하고, filter()는 "조건을 만족하는
#    원소만 골라낸다"는 차이를 basic55와 나란히 비교해 정리해준다.
# 2) [x for x in numbers if x % 2 == 0] 리스트 컴프리헨션으로도
#    동일한 결과를 만들 수 있다는 것을 함께 보여주면, map/filter와
#    컴프리헨션이 같은 목적을 위한 두 가지 문법이라는 것이 명확해진다.
# ─────────────────────────────────────────────────────────────
```

</details>

*`filter()`는 람다 함수가 True를 반환하는 원소만 남기므로, 짝수 판정 람다와 함께 쓰면 짝수만 걸러낸 새 리스트를 얻는다.*

**실행 결과**: `basic58.py`

```
[2, 4, 6]
```

---

이상으로 2.3 함수(함수 정의와 호출·람다 함수)를 마친다. 다음 절인 2.4 클래스와 객체지향 프로그래밍에서는 함수(메서드)와 데이터(속성)를 하나로 묶는 클래스와 객체를 배운다.
