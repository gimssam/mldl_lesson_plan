**2장 Python 기초 — 2.4 클래스와 객체지향 프로그래밍(OOP)**

# 2.4 클래스와 객체지향 프로그래밍 (OOP)

클래스 · 객체 · 상속 · 캡슐화

> 객체지향 프로그래밍(OOP)은 데이터(속성)와 그 데이터를 다루는 동작(메서드)을 하나의 단위인 객체로 묶어 프로그램을 구성하는 방법론이다. 클래스는 객체를 찍어내는 설계도이고, 객체는 그 설계도로 만든 실체다. 이 절에서는 클래스 정의와 `__init__`, 객체 생성, 상속과 메서드 오버라이딩, 다중 상속, 공개·보호·비공개 속성으로 데이터를 숨기는 캡슐화까지 다룬다. 원본 교재에는 실제 버그(들여쓰기 오류, 이전 셀 의존 코드)가 포함되어 있는데, 이 교안에서는 그 버그를 고치지 않고 실제 예외 그대로 재현해 원인을 함께 설명한다.

---

## 2.4.1 클래스 (Class)

- **정의**: `class` 키워드로 정의 — 객체가 가져야 할 속성과 기능(메서드)을 정의하는 틀
- **`__init__`**: 클래스가 인스턴스화될 때 자동 호출되는 초기화 메서드 — 객체의 초기 속성 설정
- **`self`**: 객체 자신을 참조하는 예약어 — 클래스 내 속성·메서드 접근 시 사용
- **구성 요소**: 속성(Attributes, `self.속성명`) · 메서드(Methods, 객체가 수행하는 동작)
- **인스턴스화**: 클래스 정의만으로는 객체가 만들어지지 않음 — `클래스명(인자)`로 인스턴스화하여 생성

---

**① 클래스 정의 — Person 클래스**

Person 클래스를 정의하고 객체를 만들어 메서드를 호출하려는 코드다.

**예제 코드**: `basic59.py`

```python
class   Person:
    def __init__(self,  name,   age):
        self.name   =   name        #   객체의  name    속성    초기화
        self.age    =   age            
    #   객체의  age 속성    초기화
    def greet(self):
        return  f"Hello,    my  name    is  {self.name} and I   am  {self.age}  years   old."

#   클래스  인스턴스(객체)  생성
person1 =   Person("Alice", 30)

#   메서드  호출
print(person1.greet())      #   출력:   Hello,  my  name    is  Alice   and I   am  30  years   old.
```


<details>
<summary><span class="label-badge">코드분석</span></summary>

```python
"""
2.4.1 클래스 — Person 클래스 정의, 책 원문의 들여쓰기 버그 재현

class 안에서 메서드들은 모두 같은 들여쓰기 폭(여기서는 공백 4칸)이어야
하는데, greet 메서드만 공백 5칸으로 한 칸 더 들여써져 있다.
"""

class Person:
    def __init__(self, name, age):   # 들여쓰기 4칸 — 정상
        self.name = name              # 객체의 name 속성 초기화
        self.age = age                # 객체의 age 속성 초기화
     def greet(self):                 # 들여쓰기 5칸 — __init__(4칸)과 어긋남
         return f"Hello, my name is {self.name} and I am {self.age} years old."

# 이 지점에서 이미 Python 인터프리터가 이 파일을 "구문 분석"하는 단계에서
# 들여쓰기 불일치를 발견해 IndentationError를 낸다 — person1 = Person(...)
# 줄까지 실행이 도달하지도 못한다.
person1 = Person("Alice", 30)
print(person1.greet())

# ─────────────────────────────────────────────────────────────
# [교안용 설명 포인트]
# 1) Python은 들여쓰기 자체가 문법이므로, 같은 블록(클래스 본문) 안의
#    모든 줄은 정확히 같은 폭으로 들여써야 한다 — 공백 1칸의 차이도
#    허용되지 않는다는 것을 이 오류로 생생하게 보여줄 수 있다.
# 2) 에디터의 탭/스페이스 혼용이나 복사·붙여넣기 과정에서 이런
#    들여쓰기 불일치가 자주 생긴다 — VS Code 같은 편집기의 자동
#    들여쓰기 표시 기능을 활용하라는 실무 팁으로 연결하면 좋다.
# ─────────────────────────────────────────────────────────────
```

</details>

---

**② 클래스의 상속 미리보기 — Student(Person)**

Person을 상속받는 Student 클래스를 정의하는 코드다.

**예제 코드**: `basic60.py`

```python
# 부모 클래스 정의

class Person:
    def __init__(self, name, age):
        self.name = name
        self.age = age
    def greet(self):
        return f"Hello, my name is {self.name} and I am {self.age} years old."
# Person 클래스를 상속받은 Student 클래스

class Student(Person):
    def __init__(self, name, age, student_id):
        super().__init__(name, age)  # 부모 클래스(Person)의 __init__ 메서드 호출
        self.student_id = student_id

    def study(self):
        return f"{self.name} is studying."
# Student 클래스 인스턴스 생성
student1 = Student("Charlie", 20, "S12345")

# 메서드 호출 및 출력
print(student1.greet())  # 부모 클래스의 메서드 호출
print(student1.study())  # 자식 클래스의 메서드 호출
```


<details>
<summary><span class="label-badge">코드분석</span></summary>

```python

# ─────────────────────────────────────────────────────────────
# [교안용 설명 포인트]
# 1) class Student(Person): 이라는 줄 자체가 "Person이라는 이름이
#    지금 이 시점에 존재해야 한다"는 요구이므로, 이 파일만 단독으로
#    실행하면 Python이 곧바로 Person을 찾지 못해 NameError를 낸다.
# 2) 노트북(주피터) 환경에서는 이전 셀에서 정의한 클래스·변수가
#    메모리에 남아 다음 셀에서도 쓸 수 있지만, .py 파일을 각각
#    독립적으로 실행할 때는 그 파일 안(또는 import한 모듈 안)에
#    정의가 있어야만 한다는 차이를 반드시 짚어줘야 한다.
# 3) 실제로 실행하려면 이 파일 맨 위에 basic59의 Person 클래스
#    정의(들여쓰기 버그를 고친 버전)를 그대로 붙여넣어야 한다는
#    것을 실습으로 보여주면 이해가 확실해진다.
# ─────────────────────────────────────────────────────────────
```

</details>
![[Pasted image 20260831221816.png]]
**실행 결과 — Student(Person) 상속**


---

## 2.4.2 객체 (Object)

- **정의**: 클래스에서 정의한 속성·메서드를 가진 실제 인스턴스 — 클래스는 설계도, 객체는 실체
- **객체 생성**: `클래스명(인자1, 인자2)` 형태로 `__init__`에 필요한 인자를 전달하여 생성
- **객체와 클래스 관계**: 여러 객체는 같은 메서드를 공유하지만, 속성 값은 서로 독립적으로 유지
- **객체를 쓰는 이유**: 구조화된 코드 · 재사용성(같은 클래스로 여러 객체 생성) · 확장성(상속을 통한 기능 확장)

---

**① 객체 생성 — Person 두 개의 인스턴스**

들여쓰기 버그를 바로잡은 온전한 Person 클래스로, 서로 다른 값을 가진 두 개의 객체를 만드는 예제다.

**예제 코드**: `basic61.py`

```python
class Person:
    def __init__(self, name, age):
        self.name = name # 객체의 name 속성
        self.age = age    # 객체의 age 속성
    def greet(self):
        return f"Hello, my name is {self.name} and I am {self.age} years old."

# Person 클래스를 사용하여 객체 생성
person1 = Person("Alice", 30)
person2 = Person("Bob", 25)

# 각 객체에서 메서드 호출
print(person1.greet()) # 출력: Hello, my name is Alice and I am 30 years old.
print(person2.greet()) # 출력: Hello, my name is Bob and I am 25 years old.
```

<details>
<summary><span class="label-badge">코드분석</span></summary>

```python
"""
2.4.2 객체 — 같은 클래스로 서로 독립적인 객체 두 개 생성

person1과 person2는 같은 Person "설계도"로 만들어졌지만, 각자
자신만의 name, age 값을 따로 가진다.
"""

class Person:
    def __init__(self, name, age):
        self.name = name   # self는 "지금 만들어지는 바로 그 객체"를 가리킴
        self.age = age
    def greet(self):
        return f"Hello, my name is {self.name} and I am {self.age} years old."

person1 = Person("Alice", 30)   # __init__(self=person1, name="Alice", age=30) 실행
person2 = Person("Bob", 25)     # __init__(self=person2, name="Bob", age=25) 실행

print(person1.greet())   # person1.greet() 안에서 self는 person1을 가리킴
print(person2.greet())   # person2.greet() 안에서 self는 person2를 가리킴

# ─────────────────────────────────────────────────────────────
# [교안용 설명 포인트]
# 1) person1.greet()와 person2.greet()는 "같은 코드"를 실행하지만
#    self가 가리키는 대상이 다르기 때문에 결과가 다르다 — self가
#    "이 메서드를 호출한 객체 자신"이라는 것을 다시 강조한다.
# 2) person1.age = 100처럼 특정 객체의 속성만 바꿔도 person2에는
#    영향이 없다는 것을 실습으로 보여주면, "객체마다 독립된 상태를
#    가진다"는 개념이 확실해진다.
# ─────────────────────────────────────────────────────────────
```

</details>

*person1과 person2는 같은 Person 클래스로 만들어졌지만, 각자 다른 name·age 값을 독립적으로 가지고 있어 `greet()`의 결과도 서로 다르게 나온다.*

**실행 결과**: `basic61.py`

```
Hello, my name is Alice and I am 30 years old.
Hello, my name is Bob and I am 25 years old.
```

---

**② 객체의 메서드 활용 — Car 클래스**

세 개의 속성(make, model, year)을 가진 Car 클래스로 두 대의 자동차 객체를 만드는 예제다.

**예제 코드**: `basic62.py`

```python
class Car:
    def __init__(self, make, model, year):
        self.make = make
        self.model = model
        self.year = year
    def car_info(self):
        return f"This car is a {self.year} {self.make} {self.model}."

# Car 클래스를 사용하여 객체 생성
car1 = Car("Toyota", "Camry", 2022)
car2 = Car("Honda", "Civic", 2021)

# 각 객체에서 메서드 호출
print(car1.car_info()) # 출력: This car is a 2022 Toyota Camry.
print(car2.car_info()) # 출력: This car is a 2021 Honda Civic.
```

<details>
<summary><span class="label-badge">코드분석</span></summary>

```python
"""
2.4.2 객체 — 속성 3개를 가진 Car 클래스로 객체 활용 복습
"""

class Car:
    def __init__(self, make, model, year):
        self.make = make      # 제조사
        self.model = model    # 모델명
        self.year = year      # 연식
    def car_info(self):
        return f"This car is a {self.year} {self.make} {self.model}."

car1 = Car("Toyota", "Camry", 2022)
car2 = Car("Honda", "Civic", 2021)

print(car1.car_info())   # This car is a 2022 Toyota Camry.
print(car2.car_info())   # This car is a 2021 Honda Civic.

# ─────────────────────────────────────────────────────────────
# [교안용 설명 포인트]
# 1) basic61의 Person과 구조가 완전히 동일하다(속성 개수만 다름) —
#    "클래스는 속성이 몇 개든, 어떤 이름이든 같은 패턴(__init__ +
#    self.속성 + 메서드)으로 만든다"는 일반화된 감각을 심어준다.
# ─────────────────────────────────────────────────────────────
```

</details>

*Person 예제와 동일한 패턴(`__init__`으로 속성 초기화, 메서드로 정보 반환)을 속성이 3개인 Car 클래스에 그대로 적용한 것이다.*

**실행 결과**: `basic62.py`

```
This car is a 2022 Toyota Camry.
This car is a 2021 Honda Civic.
```

---

## 2.4.3 상속 (Inheritance)

- **정의**: 기존 클래스(부모)의 속성·메서드를 자식 클래스가 물려받는 기능 — 코드 재사용·확장
- **기본 문법**: `class 자식클래스(부모클래스):` / `super().__init__(...)`으로 부모 초기화 메서드 호출
- **오버라이딩**: 자식 클래스가 부모 클래스의 메서드를 재정의 — 다형성(Polymorphism)의 기반
- **장점**: 코드 재사용 · 유지보수 용이(부모 수정이 자식에 자동 반영) · 확장성
- **다중 상속**: Python은 하나의 자식 클래스가 여러 부모 클래스를 동시에 상속받는 것을 지원

---

**① 상속 기본 — Animal → Dog**

부모 클래스 Animal과, 이를 상속받아 `speak` 메서드를 재정의(오버라이딩)하는 자식 클래스 Dog를 함께 정의하는 예제다.

**예제 코드**: `basic63.py`

```python
# 부모 클래스: Animal
class Animal:
    def __init__(self, name):
        self.name = name
    def speak(self):
        print(f"{self.name} makes a sound.")

# 자식 클래스: Dog (Animal을 상속)
class Dog(Animal):
    def __init__(self, name, breed):
        super().__init__(name) # 부모 클래스의 __init__ 메서드 호출
        self.breed = breed
    def speak(self):
        print(f"{self.name} barks.")

# 객체 생성
animal = Animal("Generic Animal")
dog = Dog("Rex", "Labrador")

# 메서드 호출
animal.speak() # 출력: Generic Animal makes a sound.
dog.speak()     # 출력: Rex barks.
```

<details>
<summary><span class="label-badge">코드분석</span></summary>

```python
"""
2.4.3 상속 — Animal(부모) → Dog(자식), super()와 메서드 오버라이딩
"""

class Animal:
    def __init__(self, name):
        self.name = name
    def speak(self):
        print(f"{self.name} makes a sound.")

class Dog(Animal):                     # 괄호 안 Animal → Dog가 Animal을 상속
    def __init__(self, name, breed):
        super().__init__(name)          # 부모 Animal의 __init__을 그대로 호출해 name 설정
        self.breed = breed               # Dog만의 새로운 속성 추가
    def speak(self):                     # 부모의 speak를 "같은 이름으로 다시 정의" → 오버라이딩
        print(f"{self.name} barks.")

animal = Animal("Generic Animal")
dog = Dog("Rex", "Labrador")

animal.speak()   # Animal의 speak 그대로 실행 → Generic Animal makes a sound.
dog.speak()       # Dog가 재정의한 speak가 실행됨(부모 것은 무시) → Rex barks.

# ─────────────────────────────────────────────────────────────
# [교안용 설명 포인트]
# 1) super().__init__(name)을 쓰지 않았다면 self.name = name을
#    Dog 안에서 또 써야 했을 것 — super()로 부모의 초기화 로직을
#    "재사용"한다는 점이 상속의 핵심 이점 중 하나임을 강조한다.
# 2) dog.speak()가 Animal의 speak가 아니라 Dog의 speak를 실행하는
#    것을 "오버라이딩(재정의)"이라 부르며, 이것이 basic64의
#    다형성 개념으로 이어진다.
# ─────────────────────────────────────────────────────────────
```

</details>

*Dog는 Animal을 상속받아 `name` 속성과 초기화 로직은 그대로 물려받고, `speak` 메서드만 자신만의 동작으로 다시 정의(오버라이딩)했다.*

**실행 결과**: `basic63.py`

```
Generic Animal makes a sound.
Rex barks.
```

---

**② 다형성과 메서드 오버라이딩 — Cat(Animal)**

Animal을 상속받는 또 다른 자식 클래스 Cat을 정의하는 예제다.

**예제 코드**: `basic64.py`

```python
# 부모 클래스 정의

class Animal:
    def __init__(self, name):
        self.name = name
        
    def speak(self):
        print(f"{self.name} makes a sound.")
# Animal 클래스를 상속받은 Cat 클래스

class Cat(Animal):
    def __init__(self, name, color):
        super().__init__(name)  # 부모 클래스의 __init__ 호출
        self.color = color

    def speak(self):  # 메서드 오버라이딩 (Method Overriding)
        print(f"{self.name} meows.")

# 객체 생성 및 실행
cat = Cat("Whiskers", "black")
cat.speak()  # 출력: Whiskers meows.
```



<details>
<summary><span class="label-badge">코드분석</span></summary>

```python
# ─────────────────────────────────────────────────────────────
# [교안용 설명 포인트]
# 1) basic60(Student→Person)과 완전히 같은 유형의 문제다 — 상속을
#    선언하는 class Cat(Animal):에서 Animal이라는 이름을 이 파일
#    안에서 찾지 못해 NameError가 발생한다.
# 2) 만약 Dog와 Cat이 모두 Animal을 상속받아 speak()를 각자 다르게
#    구현한다면, animal_list = [dog, cat]처럼 리스트에 담아
#    for a in animal_list: a.speak()로 순회했을 때 "같은 메서드
#    호출인데 실제 동작은 객체마다 다르게" 나타난다 — 이것이
#    다형성(Polymorphism)의 핵심 개념이라는 것을, 정상 실행되는
#    상태를 가정해 설명해주면 좋다.
# ─────────────────────────────────────────────────────────────
```

</details>
![[Pasted image 20260831222247.png]]
**실행 결과 — Cat(Animal) 다형성**

---

**③ 다중 상속 — Whale(Mammal, Swimmer)**

서로 다른 두 부모 클래스(Mammal, Swimmer)를 동시에 상속받는 다중 상속 예제다.

**예제 코드**: `basic65.py`

```python
class Mammal:
    def breathe(self):
        print("Breathing...")

class Swimmer:
    def swim(self):
        print("Swimming...")

# 자식 클래스: Whale (Mammal과 Swimmer를 상속)
class Whale(Mammal, Swimmer):
    def speak(self):
        print("The whale sings.")

# 객체 생성
whale = Whale()
whale.speak()    # 출력: The whale sings.
whale.breathe()  # 출력: Breathing...
whale.swim()     # 출력: Swimming...
```

<details>
<summary><span class="label-badge">코드분석</span></summary>

```python
"""
2.4.3 상속 — 다중 상속(class Whale(Mammal, Swimmer))

괄호 안에 부모 클래스를 콤마로 여러 개 나열하면, 그 부모들의
메서드를 모두 물려받는 자식 클래스가 된다.
"""

class Mammal:
    def breathe(self):
        print("Breathing...")

class Swimmer:
    def swim(self):
        print("Swimming...")

class Whale(Mammal, Swimmer):   # Mammal과 Swimmer 둘 다 상속(다중 상속)
    def speak(self):             # Whale만의 새로운 메서드
        print("The whale sings.")

whale = Whale()       # __init__을 따로 정의하지 않았으므로 기본 생성자 사용
whale.speak()          # Whale 자신의 메서드 → The whale sings.
whale.breathe()         # Mammal에게서 물려받은 메서드 → Breathing...
whale.swim()             # Swimmer에게서 물려받은 메서드 → Swimming...

# ─────────────────────────────────────────────────────────────
# [교안용 설명 포인트]
# 1) Whale은 Mammal도 Swimmer도 아니지만, "둘의 기능을 모두 가진
#    새로운 클래스"를 다중 상속으로 손쉽게 만들 수 있다는 것이 이
#    예제의 핵심이다.
# 2) Whale에는 __init__이 없으므로 Whale()처럼 인자 없이 호출해야
#    하며, Mammal·Swimmer 어느 쪽에도 __init__이 없어 문제없이
#    동작한다는 것도 짚어줄 수 있다.
# 3) 여러 부모가 같은 이름의 메서드를 가지고 있으면 어떤 것이
#    우선하는지(MRO, 메서드 결정 순서)는 심화 주제로 간단히
#    언급만 하고 넘어가면 좋다.
# ─────────────────────────────────────────────────────────────
```

</details>

*Whale은 Mammal과 Swimmer 두 부모 클래스를 동시에 상속받아, 자신만의 `speak`뿐 아니라 두 부모의 `breathe`, `swim` 메서드까지 모두 사용할 수 있다.*

**실행 결과**: `basic65.py`

```
The whale sings.
Breathing...
Swimming...
```

---

## 2.4.4 캡슐화 (Encapsulation)

- **정의**: 객체의 상태(속성)와 동작(메서드)을 하나로 묶고, 내부 구현을 외부로부터 숨기는 기법
- **3가지 접근 수준**: 공개(Public, `self.속성`) · 보호(Protected, `self._속성`) · 비공개(Private, `self.__속성`)
- **구현 방식**: Python은 private/protected를 명시적으로 지원하지 않지만 언더스코어(`_`, `__`) 관례로 구현
- **장점**: 데이터 보호 · 변경의 용이성 · 보안성 강화 · 코드 유지보수 용이

---

**① 캡슐화 — Public/Protected/Private 속성**

공개·보호·비공개 세 가지 접근 수준의 속성과, 그 속성을 안전하게 읽고 쓰기 위한 getter/setter 메서드를 함께 정의하는 예제다.

**예제 코드**: `basic66.py`

```python
class Person:
    def __init__(self, name, age):
        self.name = name        # 공개 속성
        self._age = age         # 보호된 속성
        self.__address = None   # 비공개 속성

    # 비공개 속성에 접근하기 위한 메서드 (getter)
    def get_address(self):
        return self.__address

    # 비공개 속성에 값을 설정하는 메서드 (setter)
    def set_address(self, address):
        self.__address = address

    # 보호된 속성에 접근하기 위한 메서드 (getter)
    def get_age(self):
        return self._age

    # 나이 값을 설정하는 메서드
    def set_age(self, age):
        if age < 0:
            print("나이는 음수가 될 수 없습니다.")
        else:
            self._age = age

    # 사람 소개하는 메서드
    def introduce(self):
        print(f"안녕하세요! 제 이름은 {self.name}이고, 나이는 {self._age}입니다.")

# 객체 생성
person = Person("John", 30)

# 공개 속성에 직접 접근
print(person.name)   # 출력: John
# 보호된 속성에 접근하려면 메서드를 사용
print(person.get_age())   # 출력: 30
# 비공개 속성에 접근하려면 메서드를 사용
person.set_address("서울시 강남구")
print(person.get_address())   # 출력: 서울시 강남구
# 나이를 음수로 설정하려고 시도
person.set_age(-5)   # 출력: 나이는 음수가 될 수 없습니다.
```

<details>
<summary><span class="label-badge">코드분석</span></summary>

```python
"""
2.4.4 캡슐화 — 공개(self.x) / 보호(self._x) / 비공개(self.__x) 속성과
getter/setter를 통한 안전한 접근

Python은 진짜 "접근 금지"를 강제하지는 않지만, 이름 앞의 밑줄 개수로
"이렇게는 쓰지 말아 달라"는 관례적 신호를 준다.
"""

class Person:
    def __init__(self, name, age):
        self.name = name        # 밑줄 없음 → 공개(Public): 어디서든 자유롭게 접근·수정 가능
        self._age = age         # 밑줄 1개 → 보호(Protected): "가급적 클래스 안에서만" 접근 권장
        self.__address = None   # 밑줄 2개 → 비공개(Private): 클래스 밖에서 이름 그대로는 접근 불가
                                  # (실제로는 _Person__address로 이름이 바뀌는 "네임 맹글링"이 적용됨)

    def get_address(self):       # __address를 클래스 밖에서 읽기 위한 전용 통로(getter)
        return self.__address

    def set_address(self, address):   # __address 값을 클래스 밖에서 바꾸기 위한 전용 통로(setter)
        self.__address = address

    def get_age(self):
        return self._age

    def set_age(self, age):      # 단순 대입이 아니라, "음수는 안 된다"는 검증 로직을 거침
        if age < 0:
            print("나이는 음수가 될 수 없습니다.")
        else:
            self._age = age

    def introduce(self):
        print(f"안녕하세요! 제 이름은 {self.name}이고, 나이는 {self._age}입니다.")

person = Person("John", 30)

print(person.name)          # 공개 속성은 person.name처럼 바로 접근 가능 → John
print(person.get_age())     # 보호 속성은 관례상 get_age() 메서드를 통해 조회 → 30

person.set_address("서울시 강남구")   # 비공개 속성은 setter로만 값을 설정
print(person.get_address())            # 비공개 속성은 getter로만 값을 조회 → 서울시 강남구

person.set_age(-5)   # 음수를 넣으려 하면 set_age 내부 검증에서 걸러짐 → 안내 메시지만 출력, 값은 그대로 30

# ─────────────────────────────────────────────────────────────
# [교안용 설명 포인트]
# 1) set_age(-5)를 호출해도 예외 없이 그냥 "안내 메시지"만 출력되고,
#    실제 _age 값은 30 그대로 유지된다는 점 — get_age()로 다시
#    확인해보면 여전히 30이라는 것을 직접 실습해보게 하면 캡슐화가
#    "잘못된 값이 들어오는 것을 막아준다"는 실질적 효과를 체감시킬
#    수 있다.
# 2) person.__address처럼 클래스 밖에서 비공개 속성에 직접 접근하려
#    하면 AttributeError가 발생한다는 것을(반면 person._Person__address
#    처럼 "맹글링된" 이름으로는 여전히 접근할 수 있어 완전한 차단은
#    아니라는 것도) 심화 학습자에게 소개하면 좋다.
# 3) person._age처럼 보호 속성은 클래스 밖에서도 접근 자체는 가능하지만
#    "관례상 하지 않는 것"일 뿐이라는 점 — Python의 캡슐화는 언어
#    차원의 강제가 아니라 "동의된 규칙"에 가깝다는 것을 강조한다.
# ─────────────────────────────────────────────────────────────
```

</details>

*공개 속성 `name`은 밖에서 바로 접근할 수 있지만, 보호 속성 `_age`와 비공개 속성 `__address`는 각각 getter/setter 메서드를 거쳐서만 안전하게 읽고 쓸 수 있다. `set_age(-5)`처럼 잘못된 값을 넣으려 하면 검증 로직이 이를 막아 원래 값을 그대로 지킨다.*

**실행 결과**: `basic66.py`

```
John
30
서울시 강남구
나이는 음수가 될 수 없습니다.
```

---

이상으로 2.4 클래스와 객체지향 프로그래밍(클래스·객체·상속·캡슐화)을 마친다. 다음 절인 2.5 라이브러리 사용에서는 NumPy·pandas·matplotlib·seaborn 등 외부 라이브러리가 제공하는 클래스와 객체(배열, 데이터프레임, 그래프)를 실제로 활용하는 방법을 배운다.
